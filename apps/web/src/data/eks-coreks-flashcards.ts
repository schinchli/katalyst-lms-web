import type { FlashcardDeck } from './flashcards';

// ═══════════════════════════════════════════════════════════════════════
// EKS Course (200-COREKS) — 9 flashcard decks, 365 cards total
// Generated from course-flashcards/module-XX/index.html
// ═══════════════════════════════════════════════════════════════════════

export const eksCoreksFlashcardDecks: FlashcardDeck[] = [
  {
    id: 'eks-coreks-m01',
    title: 'EKS M01: Kubernetes Fundamentals',
    description: 'Core K8s — Pods, Deployments, Services, Probes, Rolling Updates',
    category: 'eks-coreks',
    cardCount: 63,
    color: '#4A90E2',
    icon: '☸️',
    cards: [
      { id: '1', front: `What does it mean that containers are portable ?`, back: `Containers run on any OS that supports the containerization platform. You can also run multiple versions of the same app, each with its own dependencies, side-by-side. Analogy: shipping containers ride on trucks, trains and ships unchanged.` },
      { id: '2', front: `What makes containers cloud deployable ?`, back: `Containers are lightweight and self-contained, so they fit naturally into managed cloud platforms. On AWS that means Amazon EC2 , AWS Lambda , Amazon ECS , Amazon EKS , and AWS App Runner — each runs containerized apps with a different operational model.` },
      { id: '3', front: `What does scalable mean for containers?`, back: `Because containers are small and start fast, you can scale out by adding more copies and scale in by removing them — without changing your build tools, architecture, or development workflow. Scaling is just "add another instance," not "rebuild the app."` },
      { id: '4', front: `Why are containers good for continuous deployment ?`, back: `A container image is identical in dev, QA, and prod — only the host OS and infrastructure change. That minimises divergence between environments and integrates cleanly with automated build → test → deploy pipelines.` },
      { id: '5', front: `What is a declarative format ?`, back: `You describe the result you want , not the steps to produce it. Drawing a rectangle imperatively means drawLine ×4; declaratively it's { shape: "rect", w: 300, h: 100 } . Kubernetes manifests are declarative — you say "I want 4 replicas" and let the controller reconcile.` },
      { id: '6', front: `Monolith vs microservices — what's the structural difference?`, back: `A monolith is one program that does everything: UI, business logic, data. Each app duplicates shared concerns (order history, mailing). A microservice architecture splits the app into many small, independently-deployable services — e.g., one ordering service, one reporting service — each owning its own concerns.` },
      { id: '7', front: `Why are containers a good fit for microservices?`, back: `Microservices need to be independently deployable , independently scalable , and portable across environments. Those are exactly the properties containers give you out of the box.` },
      { id: '8', front: `When do containers stop being simple to manage?`, back: `Running 1–2 containers on one host is trivial. Once you move to dozens of hosts and hundreds of containers — let alone production with thousands — you can no longer place, monitor, or restart them manually. That's the problem orchestration solves.` },
      { id: '9', front: `What does a container orchestration tool automate?`, back: `Scheduling placement · cloud integration · networking · load balancing · storage · security · monitoring · logging. You declare desired state; the orchestrator makes the cluster match it.` },
      { id: '10', front: `What does Kubernetes add on top of containers?`, back: `All the orchestration concerns — scheduling, scaling, load balancing, deployments — plus a strong community and a declarative API. Kubernetes is the substrate; EKS is the AWS-managed flavour.` },
      { id: '11', front: `What is a Pod?`, back: `A group of one or more containers that share the same network namespace (IP, port space) and can share storage. The smallest deployable unit in Kubernetes — and the basic building block for deployment, scaling and replication.` },
      { id: '12', front: `What's a PodSpec?`, back: `The YAML specification that describes how to run the containers in a Pod — image, ports, env, volumes. Stored declaratively and submitted to the API server.` },
      { id: '13', front: `How do containers inside the same Pod talk to each other?`, back: `They share a network namespace, so they can talk via localhost and shared volumes. They're co-scheduled to the same node and live and die together.` },
      { id: '14', front: `How do containers in different Pods talk to each other?`, back: `Each Pod gets its own cluster IP. Pods can hit each other directly by IP — but those IPs change when Pods restart, so you almost always go through a Service instead.` },
      { id: '15', front: `What is a Kubernetes Service?`, back: `A logical collection of Pods plus a stable way to reach them. The Service has its own IP and port; it's continuously updated with the set of Pod IPs that currently match its selector, so callers never need to track individual Pods.` },
      { id: '16', front: `Why can't apps just call Pods by IP?`, back: `Pod IPs are ephemeral . A Pod crashes → it gets recreated with a new IP. A Service IP is stable and load-balances across whichever Pods currently match its label selector.` },
      { id: '17', front: `What's a node? How does it relate to a Pod?`, back: `A node is a machine (VM or physical) that runs Pods. A Kubernetes cluster is the set of nodes. Pods are always scheduled to exactly one node — containers within a Pod cannot be split across nodes.` },
      { id: '18', front: `How do nodes relate to Availability Zones?`, back: `In EKS, nodes live in AWS subnets, which live in specific AZs (e.g., us-east-1a , us-east-1b ). Spreading nodes across multiple AZs is how you survive an AZ failure. The scheduler can place Pods AZ-aware via topology spread affinity rules.` },
      { id: '19', front: `What is the control plane ?`, back: `The set of components that manage the cluster : schedule Pods, detect failures, react to events. Components: API server , etcd , scheduler , controller manager , cloud controller .` },
      { id: '20', front: `What is the data plane ?`, back: `The set of worker nodes that actually run your containerized applications. Each node runs a container runtime, kubelet , and kube-proxy .` },
      { id: '21', front: `What is a controller ?`, back: `A loop that compares desired state (from your manifests) to current state (what's actually running). If they differ, the controller acts to make them match. If they match, it does nothing.` },
      { id: '22', front: `What does the API server do?`, back: `It's the front door of the cluster. Everything — kubectl, kubelet, controllers — talks to the API server. It validates and persists cluster state to etcd, and is the only component that reads/writes etcd.` },
      { id: '23', front: `What is etcd ?`, back: `A consistent, distributed key-value store . It holds the entire cluster state — every Pod spec, every config, every secret. Lose etcd and you lose the cluster's memory.` },
      { id: '24', front: `What does the scheduler do?`, back: `When a new Pod is created without a node assignment, the scheduler picks one. It uses predicates to filter ineligible nodes and priorities to score the survivors. The highest-scoring node wins.` },
      { id: '25', front: `What does the controller manager do?`, back: `It runs many built-in controllers in one process — Deployment controller, ReplicaSet controller, Node controller, Endpoint controller, Job controller, and others. Each runs its own control loop.` },
      { id: '26', front: `What does the cloud controller do?`, back: `A controller that integrates with the underlying cloud provider : it manages cloud-specific resources like load balancers, routes, and volumes (on AWS: ELB, EBS attachments, route tables) on behalf of the cluster.` },
      { id: '27', front: `What is the kubelet ?`, back: `The primary node agent . It watches the API server for Pods assigned to its node, then asks the container runtime to start them. It also reports node and Pod health back to the control plane.` },
      { id: '28', front: `What is kube-proxy ?`, back: `A small per-node process that programs networking rules (iptables / IPVS) so that traffic to a Service IP gets routed to one of its backing Pods. Without it, Services wouldn't actually route traffic.` },
      { id: '29', front: `What is the container runtime ?`, back: `The software that actually starts, stops, and isolates containers on a node. Kubernetes supports several via the CRI interface; EKS uses containerd by default since Kubernetes v1.24 (Docker Engine is deprecated).` },
      { id: '30', front: `How does the control plane talk to the data plane?`, back: `Through the API server. The kubelet on each node watches the API server for Pods assigned to its node. There is no direct controller→kubelet channel — everything is mediated by the API server, which is the cluster's single source of truth.` },
      { id: '31', front: `Predicates vs priorities — what's the difference?`, back: `Predicates are filters: "can this Pod possibly run here?" If no, the node is excluded. Priorities are scores: "of the survivors, which is the best home?" Highest score wins.` },
      { id: '32', front: `What is a requests setting on a container?`, back: `The minimum guaranteed CPU and memory a container will get. The scheduler uses requests to pick a node with enough free capacity. Example: cpu: 400m, memory: 600Mi .` },
      { id: '33', front: `What is a limits setting on a container?`, back: `The maximum a container is allowed to use. CPU above the limit is throttled; memory above the limit gets the container OOMKilled . Limits are enforced at runtime; requests influence scheduling.` },
      { id: '34', front: `What is a taint ?`, back: `A property of a node that repels Pods. Only Pods that explicitly tolerate the taint can be scheduled there. Example taint: spot=true:NoSchedule means "don't schedule unless you tolerate spot instances."` },
      { id: '35', front: `What is a toleration ?`, back: `A property of a Pod that lets it ignore matching taints. Tolerations don't force placement — they only permit it. Pairs with affinity to actually attract.` },
      { id: '36', front: `What is node affinity ?`, back: `A Pod rule that attracts the Pod to nodes matching labels. requiredDuringScheduling… is a hard filter (no match → no schedule). preferredDuringScheduling… is a soft score (boost matching nodes but don't require them).` },
      { id: '37', front: `How is a volume requirement a predicate?`, back: `An EBS volume in us-east-1a can't attach to a node in us-east-1b . So the volume binding effectively constrains which nodes are even eligible for the Pod. Similarly, a Pod needing a volume already mounted on node X must go to node X.` },
      { id: '38', front: `When you combine taints/tolerations with affinity — what do you get?`, back: `Dedicated nodes. Taint a node so generic workloads avoid it; add a matching toleration + affinity to specific Pods so only they land there. Classic use: GPU nodes, spot pools, system add-ons.` },
      { id: '39', front: `What is kubectl ?`, back: `The command-line interface for communicating with the Kubernetes API server. It runs on your laptop, your CI agent, anywhere — it just needs network access to a cluster and a kubeconfig.` },
      { id: '40', front: `How does kubectl know which cluster to talk to?`, back: `Through a kubeconfig file , by default at ~/.kube/config . Override the path with the KUBECONFIG env var. Within a file, multiple contexts let you switch clusters with kubectl config use-context .` },
      { id: '41', front: `What does kubectl get pods do?`, back: `Lists Pods in the current namespace (default: default ). Add -n my-ns to target a different namespace, or -A to see Pods across all namespaces.` },
      { id: '42', front: `What does kubectl apply -f file.yaml do?`, back: `Sends the manifest to the API server, which creates or updates the matching object to match the file. Idempotent: running it twice is safe; controllers reconcile to the new desired state.` },
      { id: '43', front: `How do you create a namespace from the CLI?`, back: `kubectl create namespace my-ns Imperative shortcut. The declarative equivalent is a one-line YAML manifest applied with kubectl apply .` },
      { id: '44', front: `If you deploy into my-ns but run kubectl get pods , what happens?`, back: `You'll see " No resources found in default namespace " — because get pods defaults to default . Run kubectl get pods -n my-ns instead, or set the context's namespace.` },
      { id: '45', front: `What is a Namespace?`, back: `A virtual cluster inside a physical cluster. Names must be unique within a namespace, but the same name can exist in different namespaces. Combined with RBAC, namespaces give multi-tenant isolation.` },
      { id: '46', front: `When are Namespaces especially useful?`, back: `When multiple teams or projects share a cluster . Each team gets its own namespace; RBAC controls who can read/write it. Resource quotas can also be applied per namespace.` },
      { id: '47', front: `What is a ConfigMap?`, back: `An API object storing non-confidential configuration as key-value pairs. Pods mount it as files or read it as env vars. Lets the same container image run in dev and prod with different config.` },
      { id: '48', front: `What is a Secret?`, back: `Same shape as a ConfigMap but for sensitive data (passwords, tokens, keys). Values are stored base64-encoded in the data field — note that base64 is encoding, not encryption. At-rest encryption requires enabling etcd encryption.` },
      { id: '49', front: `Why separate config from the container image?`, back: `So the same image runs unchanged from dev to prod. The image is the artifact ; ConfigMaps and Secrets are the environment-specific overlay . This is the Twelve-Factor "config in the environment" principle.` },
      { id: '50', front: `How does a Pod consume a ConfigMap?`, back: `Two common ways: (1) mounted as files in a read-only volume (each key becomes a file); (2) injected as environment variables via envFrom.configMapRef . The Pod's container code reads them like any other file or env var.` },
      { id: '51', front: `What is a Custom Resource (CR)?`, back: `A user-defined object type, declared via a Custom Resource Definition (CRD) . Extends the Kubernetes API. With a custom controller watching it, you get the same reconcile-loop pattern for your own domain — e.g., a Database CR that provisions RDS.` },
      { id: '52', front: `Why use a workload instead of a bare Pod?`, back: `A bare Pod that dies stays dead . A workload controller re-creates Pods to match the desired count or schedule. Workloads also add features bare Pods don't have — rolling updates, fixed identity, scheduled execution.` },
      { id: '53', front: `What is a Job ?`, back: `A workload that runs a Pod once to completion . Good for batch tasks (DB migration, one-shot data processing). The Job tracks success/failure and will retry failed Pods up to a limit.` },
      { id: '54', front: `What is a CronJob ?`, back: `A workload that creates Jobs on a schedule , using cron syntax ( */1 * * * * = every minute). It's just a Job factory — each scheduled tick produces a fresh Job which produces a Pod.` },
      { id: '55', front: `What is a DaemonSet ?`, back: `A workload that runs one Pod on every node (or every matching node). Used for node-level agents: log collector (Fluent Bit), metrics agent, CSI node plugin, kube-proxy itself. Not supported on AWS Fargate (Fargate has no notion of a shared node).` },
      { id: '56', front: `What is a ReplicaSet ?`, back: `A workload that keeps a specific number of Pod replicas running. If a Pod dies, the ReplicaSet creates a new one. You rarely manage ReplicaSets directly — Deployments do it for you.` },
      { id: '57', front: `What is a Deployment ?`, back: `A workload that owns and manages ReplicaSets . You declare a desired state — image version, replica count — and the Deployment controller rolls out changes at a controlled rate. The standard way to run stateless apps.` },
      { id: '58', front: `How does a Deployment perform a rolling update?`, back: `When you change the Pod template, the Deployment creates a new ReplicaSet for the new version. It scales the new RS up and the old RS down in lockstep, respecting maxSurge and maxUnavailable — never both at once. Old RS sticks around for rollback.` },
      { id: '59', front: `What is a StatefulSet ?`, back: `A workload for stateful apps. Pods get stable, ordered identities ( mysql-0 , mysql-1 , …) and stable PersistentVolume bindings. Pods are created, scaled, and updated in order. Pairs with PersistentVolumes for databases, queues, leader-elected systems.` },
      { id: '60', front: `How does a Deployment know which Pods it owns?`, back: `Through the selector field, which matches labels on Pods. The Deployment's template.metadata.labels must match its selector.matchLabels — otherwise the Deployment can't manage the Pods it just created.` },
      { id: '61', front: `Deployment vs StatefulSet — when to pick which?`, back: `Deployment : stateless, interchangeable Pods — web frontends, API servers, workers reading from a queue. StatefulSet : each Pod has identity that matters — databases, Kafka brokers, Zookeeper ensembles, anything that does leader election or stores local data.` },
      { id: '62', front: `What is the basic unit of scaling and resiliency in Kubernetes?`, back: `Pods. Not containers (those live inside Pods), not Tasks (that's ECS terminology), not ReplicaSets (those manage Pods). Pods are the basic building block for deployment, scaling, and replication.` },
      { id: '63', front: `What is a Kubernetes Service?`, back: `A logical collection of Pods plus a means to access them . The Service is continuously updated with the current Pod IPs, so callers don't need to track individual Pods. (Not scheduling — that's the scheduler's job. Not DaemonSet — that's per-node. Not ReplicaSet — that's replica count.)` },
    ],
  },
  {
    id: 'eks-coreks-m02',
    title: 'EKS M02: Amazon EKS Fundamentals',
    description: 'Control plane, data plane, managed node groups, Fargate, eksctl',
    category: 'eks-coreks',
    cardCount: 36,
    color: '#F39C12',
    icon: '🔶',
    cards: [
      { id: '1', front: `What is Amazon EKS in one sentence?`, back: `Amazon EKS is a managed Kubernetes service that creates and operates the Kubernetes control plane on your behalf, so you only manage your own worker nodes (data plane).` },
      { id: '2', front: `What is Amazon EKS Distro?`, back: `EKS Distro is the Kubernetes distribution that powers EKS clusters. It is open-source, security-patched, and the same distribution AWS runs internally. You can also run it on-premises.` },
      { id: '3', front: `What are the four pillars of the EKS component set?`, back: `1. Amazon EKS — creates K8s clusters (EKS Distro) 2. AWS Fargate — serverless container nodes 3. Amazon EC2 — traditional nodes for your cluster 4. EKS Dashboard (console) — view and explore running K8s apps` },
      { id: '4', front: `What does "managed" actually mean in "managed Kubernetes"?`, back: `AWS handles control plane provisioning, scaling, patching, and high availability . You do not SSH into control plane nodes, you do not run etcd backups, you do not manage API server certificates. You just create the cluster; AWS does the rest for the control plane.` },
      { id: '5', front: `Where does the EKS control plane run?`, back: `In an AWS-managed AWS account — completely separate from your account. The control-plane EC2 instances are invisible to you. You access the cluster only via the API server endpoint that EKS exposes.` },
      { id: '6', front: `How does EKS maintain high availability for the control plane?`, back: `EKS distributes control plane nodes across multiple Availability Zones . It continuously monitors health and automatically replaces unhealthy nodes. Etcd is also replicated across AZs. You never need to perform manual failover.` },
      { id: '7', front: `Who manages etcd availability in an EKS cluster?`, back: `Amazon EKS manages etcd completely. You have no SSH access to etcd nodes and no responsibility for backups or replication. This is one of the key managed benefits — losing an etcd node does not require operator intervention.` },
      { id: '8', front: `What is the ENI injection and why does it matter?`, back: `EKS injects an Elastic Network Interface (ENI) into your VPC so the control plane can communicate with worker nodes. This means control plane → node traffic never leaves AWS's private backbone, and you control the VPC security groups around it.` },
      { id: '9', front: `What does the EKS pricing model look like?`, back: `You pay a per-cluster hourly fee for the managed control plane (~$0.10/hr). Worker nodes are billed separately as standard EC2 or Fargate compute. There is no extra charge for multi-AZ control plane redundancy — that's included.` },
      { id: '10', front: `What is the key difference between self-managed nodes and managed node groups?`, back: `Self-managed: you launch, register, patch, and replace EC2 instances manually (or via your own automation). Managed node groups: EKS handles provisioning, joining nodes to the cluster, OS patching, rolling upgrades, and scaling — all via the EKS API or console.` },
      { id: '11', front: `Which data plane option gives you zero node management?`, back: `AWS Fargate . You never see or manage worker nodes. AWS provisions an isolated micro-VM for each Pod automatically. You only define Fargate profiles to tell EKS which Pods should run on Fargate.` },
      { id: '12', front: `What does a managed node group help you with?`, back: `5 things MNG manages for you: 1. Provisioning — launches EC2 in an ASG 2. Managing — joins nodes to the cluster 3. Updating — rolling AMI/K8s version upgrades 4. Scaling — min/max/desired count controls 5. eksctl support — first-class CLI integration` },
      { id: '13', front: `How does EKS update a managed node group without downtime?`, back: `EKS performs a rolling update : it adds new nodes (new AMI/version), then cordons and drains old nodes one at a time (respecting PodDisruptionBudgets), then terminates the old nodes. Workloads are always available during the upgrade.` },
      { id: '14', front: `What CloudWatch event tells you a managed node group ran out of EC2 capacity?`, back: `The event reason InstanceLimitExceeded . EKS surfaces node group health events in the EKS console and via the DescribeNodegroup API. You should also set CloudWatch alarms on the underlying Auto Scaling Group metrics.` },
      { id: '15', front: `What tool provides first-class CLI support for managed node groups?`, back: `eksctl — the official CLI for EKS. With a single YAML config file, eksctl create cluster provisions the cluster and managed node group. You can also eksctl upgrade nodegroup to trigger a managed rolling update.` },
      { id: '16', front: `What are the five Fargate properties the course lists?`, back: `Native — first-class EKS integration Rightsized — compute matches Pod CPU/memory request Fast and simple — no node bootstrapping Transparent — Pods look like K8s Pods; no special code Optimized — no over-provisioning; pay only for what Pod requests` },
      { id: '17', front: `What is a Fargate profile and what fields does it contain?`, back: `A Fargate profile tells EKS which Pods to schedule on Fargate. Fields: • fargateProfileName • clusterName • podExecutionRoleArn — IAM role used by the Fargate runtime • subnets — private subnets only • selectors → namespace + optional labels` },
      { id: '18', front: `What subnet type is required for Fargate profiles?`, back: `Private subnets only. Fargate Pods cannot run in public subnets — this is a hard requirement. This is also one reason you would choose EC2 node groups instead of Fargate if you need Pods in public subnets.` },
      { id: '19', front: `How does Fargate decide whether to schedule a Pod?`, back: `When a Pod is created, EKS checks it against all Fargate profiles in the cluster. If the Pod's namespace and any specified labels match a profile's selector, it is scheduled on Fargate. If no profile matches, EKS schedules it on an EC2 node (if any exist).` },
      { id: '20', front: `What IAM role does a Fargate profile need and why?`, back: `The Pod Execution Role — an IAM role that the Fargate infrastructure uses to pull images from ECR and write logs to CloudWatch. It is attached to the profile, not the Pod itself. Without it, the Fargate micro-VM cannot authenticate to AWS services.` },
      { id: '21', front: `Can Fargate run privileged containers?`, back: `No. Fargate does not support privileged containers. If your Pod needs privileged: true in its security context (e.g., for DaemonSets like FluentD or host-network Pods), you must use EC2 node groups.` },
      { id: '22', front: `Why does Fargate improve security compliance?`, back: `Each Fargate Pod runs in its own isolated micro-VM (1:1 Pod-to-VM ratio). Pods cannot share the kernel or process namespace with other workloads. This eliminates container-escape concerns that exist when many Pods share an EC2 node.` },
      { id: '23', front: `Why do node groups offer faster Pod start times than Fargate?`, back: `On EC2 node groups, nodes are already running — a new Pod just needs to be scheduled and the container image pulled. On Fargate, EKS must provision a new micro-VM for each Pod, which adds cold-start latency (typically 30–60 seconds).` },
      { id: '24', front: `You want to deploy a new version of your app. Which API?`, back: `Kubernetes API via kubectl . You update the Deployment resource inside the cluster. The EKS API is not involved in workload changes — it only manages the cluster infrastructure.` },
      { id: '25', front: `You want to add a new node group to your cluster. Which API?`, back: `Amazon EKS API via eksctl create nodegroup or the AWS Console. Node groups are EKS infrastructure constructs, not Kubernetes objects. You'd never create a node group with kubectl .` },
      { id: '26', front: `What are EKS "add-ons" and which API manages them?`, back: `Add-ons are managed operational components: CoreDNS, kube-proxy, Amazon VPC CNI, and EBS CSI driver. The EKS API manages their installation and version upgrades — you no longer need to maintain them manually with kubectl apply .` },
      { id: '27', front: `What does the EKS API use to authenticate users to the K8s API?`, back: `EKS uses IAM for authentication (who you are) and maps IAM identities to Kubernetes RBAC roles for authorization (what you can do). The aws-auth ConfigMap (or newer access entries) defines those mappings. The Identity Provider Config (OIDC) enables workload identity federation.` },
      { id: '28', front: `What is an Admission Controller?`, back: `A plugin in the Kubernetes API server that intercepts requests after authentication/authorization but before the object is persisted. It can validate (allow/deny) or mutate (modify) the object. Example: enforcing that all Pods set resource limits.` },
      { id: '29', front: `What makes a MutatingAdmissionWebhook different from a regular admission controller?`, back: `A MutatingAdmissionWebhook calls an external HTTP endpoint (your webhook server) to modify objects at admission time. This allows injecting sidecars, adding annotations, or patching fields without modifying the original manifests — used by Istio, Linkerd, and tools like kyverno .` },
      { id: '30', front: `How do you generate the kubeconfig for a new EKS cluster?`, back: `aws eks update-kubeconfig --region region --name cluster-name This writes a kubeconfig entry with the cluster endpoint and CA data, using your current AWS IAM credentials to authenticate via the aws eks get-token exec credential plugin.` },
      { id: '31', front: `Why does adding a node to an EKS cluster require both an IAM role AND kubeconfig?`, back: `The IAM role allows the node (kubelet) to make AWS API calls — pull images from ECR, write logs to CloudWatch, describe itself. The kubeconfig / bootstrap token allows the kubelet to authenticate to the Kubernetes API server and register as a node object.` },
      { id: '32', front: `KC 1 — Which component is always managed by Amazon EKS? A. Control plane B. Data plane C. Container D. Service`, back: `✓ A — Kubernetes control plane EKS always manages the control plane. The data plane (B) is your responsibility (managed node groups, self-managed, or Fargate). Containers (C) and Services (D) are K8s objects you manage.` },
      { id: '33', front: `KC 2 — Who is responsible for managing the availability of etcd when using EKS? A. Amazon EKS B. Kubernetes C. You`, back: `✓ A — Amazon EKS etcd is part of the control plane, which EKS fully manages. It runs replicated across AZs and is automatically repaired. You have no etcd access and no etcd backup responsibility.` },
      { id: '34', front: `Which EKS data plane option requires private subnets only?`, back: `AWS Fargate. Fargate Pods must run in private subnets — no public subnet support. EC2 managed node groups can run in either public or private subnets.` },
      { id: '35', front: `You need to create a Fargate profile. Which field in the profile JSON specifies the IAM permissions for the Fargate runtime?`, back: `podExecutionRoleArn — this IAM role is used by the Fargate infrastructure (not your application code) to pull images from ECR, push logs to CloudWatch, and interact with the VPC.` },
      { id: '36', front: `Which tool do you use to manage Kubernetes objects (Pods, Deployments) inside an EKS cluster?`, back: `kubectl — it talks to the Kubernetes API . eksctl and the AWS CLI talk to the Amazon EKS API for infrastructure management (clusters, node groups, Fargate profiles, add-ons).` },
    ],
  },
  {
    id: 'eks-coreks-m03',
    title: 'EKS M03: Building an EKS Cluster',
    description: 'eksctl, cluster.yaml, AMI types, upgrades, add-ons',
    category: 'eks-coreks',
    cardCount: 30,
    color: '#38bdf8',
    icon: '🔧',
    cards: [
      { id: '1', front: `What are the three main interfaces for creating an EKS cluster?`, back: `1. AWS Console — guided wizard, not repeatable 2. AWS CLI — aws eks create-cluster , scriptable but verbose 3. eksctl — purpose-built CLI, creates VPC + IAM + cluster in one command` },
      { id: '2', front: `Why is the AWS Console not ideal for production cluster creation?`, back: `The Console is not repeatable — it produces no code artifact. If you need to recreate the cluster (DR, new region, staging env), you have to click through the wizard again. Use eksctl or IaC for any cluster you care about.` },
      { id: '3', front: `What does eksctl do automatically that the AWS CLI does NOT?`, back: `eksctl automatically creates: • VPC (192.168.0.0/16) with public/private subnets • IAM roles for the cluster and node group • CloudFormation stacks to manage all resources • kubeconfig entry on your local machine The AWS CLI only calls the EKS API — you supply all dependencies.` },
      { id: '4', front: `What are the 6 things eksctl creates by default when you run eksctl create cluster ?`, back: `1. IAM roles for cluster + node group 2. Dedicated VPC (192.168.0.0/16) with public private subnets 3. EKS cluster (control plane) 4. Managed node group with specified instance type 5. API endpoint access configuration 6. kubeconfig written locally for kubectl` },
      { id: '5', front: `What CIDR block does eksctl use for the automatically created VPC?`, back: `192.168.0.0/16 — eksctl's default VPC CIDR. This avoids conflicts with the common 10.0.0.0/8 and 172.16.0.0/12 ranges used in corporate networks. You can override this in cluster.yaml via the vpc.cidr field.` },
      { id: '6', front: `What is the --managed flag in eksctl and why use it?`, back: `The --managed flag creates a managed node group instead of a self-managed one. With a managed node group, EKS handles rolling updates, AMI patching, and draining — you don't manage the lifecycle yourself. It's almost always the right choice for new clusters.` },
      { id: '7', front: `What does privateNetworking: true in cluster.yaml do?`, back: `It deploys node group instances into private subnets only — nodes get no public IP addresses. Traffic between nodes and the internet routes through a NAT Gateway. This is a security best practice for production clusters.` },
      { id: '8', front: `Two ways to specify the AWS region in eksctl — what are they?`, back: `B. The --region flag on the CLI command: eksctl create cluster --region us-west-2 C. The metadata.region field in cluster.yaml: metadata: region: us-east-1` },
      { id: '9', front: `What technology does eksctl use under the hood to manage AWS resources?`, back: `AWS CloudFormation. eksctl generates and submits CloudFormation stacks for every resource it creates (VPC, EKS cluster, node group). This means all eksctl-created resources appear in the CloudFormation console and benefit from stack-level rollback.` },
      { id: '10', front: `What AMI type is open-source and purpose-built for running containers?`, back: `Bottlerocket — an open-source Linux-based OS developed by AWS. It contains only what is needed to run containers, resulting in a minimal attack surface and faster startup. Available as a built-in EKS AMI option.` },
      { id: '11', front: `What is the default EKS AMI type and what does "turnkey" mean in this context?`, back: `Amazon EKS Optimized Linux is the default. "Turnkey" means AWS pre-installs and pre-configures all EKS node components (kubelet, kube-proxy, containerd, AWS VPC CNI) — you select it and it works immediately with no additional setup.` },
      { id: '12', front: `Custom AMIs are NOT supported with which deployment type?`, back: `AWS Fargate. Fargate is serverless — AWS manages the underlying compute. You cannot bring a custom AMI because there is no node OS for you to control. Custom AMIs are only valid for EC2-based node groups (self-managed or managed).` },
      { id: '13', front: `What Windows Server versions are supported for Windows EKS nodes?`, back: `Windows Server 2019 and Windows Server 2022 . EKS supports mixed Linux/Windows clusters — you can have Linux node groups and Windows node groups in the same cluster. Pod scheduling to the right OS is handled with node selectors.` },
      { id: '14', front: `Why would you choose Bottlerocket over EKS Optimized Linux?`, back: `Security: Smaller OS footprint = smaller attack surface Immutability: The OS is read-only at runtime (harder to persist malware) Automatic updates: Atomic OS updates with rollback support Purpose-built: No general-purpose packages, only what containers need` },
      { id: '15', front: `What are EKS Blueprints and what makes them different from raw CloudFormation?`, back: `EKS Blueprints are pre-built cluster templates that include not just the EKS cluster but also operational software — cluster autoscaler, load balancer controller, metrics server, logging, etc. You get a fully bootstrapped cluster ready for production workloads, not just raw infrastructure.` },
      { id: '16', front: `What is the key advantage of using CloudFormation for EKS cluster management?`, back: `Automatic rollback on failure. If a CloudFormation stack update fails, AWS automatically rolls back all changes to the previous known-good state. Other benefits: native AWS integration, change sets for previewing updates, drift detection.` },
      { id: '17', front: `Why might a team choose Terraform over CloudFormation for EKS?`, back: `Multi-cloud: Terraform manages AWS, GCP, Azure resources in a single codebase Existing workflows: Teams already using Terraform for other infra don't need a second tool State management: Terraform state file supports complex dependency tracking across providers Community modules: Rich ecosystem of EKS modules on Terraform Registry` },
      { id: '18', front: `What is the correct order: upgrade control plane FIRST or data plane FIRST?`, back: `Control plane first, data plane second. Always upgrade the EKS control plane before upgrading worker nodes. The Kubernetes compatibility guarantee says a control plane can be one minor version ahead of nodes, but not the reverse.` },
      { id: '19', front: `Who handles patch version upgrades to the EKS control plane?`, back: `AWS applies patch versions automatically to the control plane (e.g., 1.27.1 → 1.27.4). You only control minor version upgrades (e.g., 1.27 → 1.28). This is one of the managed benefits — you never need to apply a patch for the API server.` },
      { id: '20', front: `What is the minimum upgrade type that can remove Kubernetes APIs?`, back: `B — Minor version upgrade (e.g., 1.26 → 1.27). Only major or minor version upgrades can remove or change APIs. Patch versions (1.27.1 → 1.27.2) never remove APIs. This is why step 3 (identify API changes) only matters for minor version upgrades.` },
      { id: '21', front: `Which node type requires NO manual upgrade steps during an EKS version upgrade?`, back: `AWS Fargate. Because Fargate is serverless and AWS manages the underlying compute, Fargate nodes are automatically upgraded when new Pods are scheduled. There is no node upgrade action required from you for Fargate workloads.` },
      { id: '22', front: `What happens if the EKS control plane upgrade fails mid-flight?`, back: `EKS automatically rolls back the control plane. New API server nodes are deployed alongside old ones; only after the new nodes are healthy does EKS cut over. If health checks fail, the rollback is automatic and the cluster stays on the original version.` },
      { id: '23', front: `Why must you upgrade EKS add-ons AFTER the control plane upgrade (Step 6)?`, back: `Add-ons like CoreDNS, kube-proxy, and VPC CNI have version compatibility requirements tied to the Kubernetes minor version. An old add-on version may not support new API objects introduced in the upgraded Kubernetes version — upgrading add-ons after the control plane ensures compatibility.` },
      { id: '24', front: `How do you upgrade a managed node group?`, back: `Via the AWS Console or eksctl : eksctl upgrade nodegroup \\ --name=standard-workers \\ --cluster=prod \\ --kubernetes-version=1.28 EKS performs a rolling update: new nodes → drain old nodes → terminate old nodes.` },
      { id: '25', front: `KC 1 — Which node AMI type is open-source and purpose-built for running containers? A. EKS Optimized Linux B. Bottlerocket C. Custom D. Windows`, back: `✓ B — Bottlerocket Bottlerocket is the open-source, container-specific OS developed by AWS. EKS Optimized Linux (A) is the default/turnkey option but not specifically container-only. Custom (C) and Windows (D) serve different purposes.` },
      { id: '26', front: `KC 2 — IAM permissions are provided via the node IAM role to which two entities? A. kube-apiserver B. kubelet on each node C. kube-scheduler D. Applications running in pods`, back: `✓ B and D B. kubelet on each node — uses the node IAM role to make EC2, ECR, and CloudWatch API calls. D. Applications running in pods — pods inherit the node IAM role unless IRSA (IAM Roles for Service Accounts) is configured. The API server (A) and scheduler (C) run in the AWS-managed control plane, not on your nodes.` },
      { id: '27', front: `KC 3 — How can you specify the AWS region when using eksctl? A. AWS_DEFAULT_REGION env var B. --region flag C. cluster.yaml metadata.region D. IAM role ARN`, back: `✓ B and C B. --region flag — e.g. eksctl create cluster --region us-west-2 C. cluster.yaml — metadata.region: us-east-1 AWS_DEFAULT_REGION (A) works with the AWS CLI but is not the eksctl-specific method. IAM role ARN (D) is for authentication, not region selection.` },
      { id: '28', front: `KC 4 — What is the minimum upgrade type that can result in the removal of an API? A. Patch version B. Minor version C. Major version D. AMI update`, back: `✓ B — Minor version upgrade Kubernetes follows semver: only minor version upgrades (e.g. 1.27 → 1.28) can deprecate and remove APIs. Patch versions (A) are backward-compatible bug fixes. Major versions (C) would also remove APIs but EKS doesn't ship major K8s changes independently. AMI updates (D) don't change the Kubernetes API.` },
      { id: '29', front: `You run eksctl create cluster --name demo --managed with no other flags. What VPC CIDR does eksctl use?`, back: `192.168.0.0/16 — eksctl's built-in default VPC CIDR. It also creates subnets across two AZs and configures both public and private subnets. You can override this with vpc.cidr in cluster.yaml.` },
      { id: '30', front: `What is step 5 in the EKS upgrade sequence, and what technology does EKS use to execute it safely?`, back: `Step 5 is "Upgrade the cluster control plane" . EKS deploys new API server nodes alongside old ones, waits for them to be healthy, then cuts over. If new nodes fail health checks, EKS automatically rolls back to the old nodes — no manual intervention needed.` },
    ],
  },
  {
    id: 'eks-coreks-m04',
    title: 'EKS M04: Deploying Apps to EKS',
    description: 'ECR, image scanning, Helm charts, CI/CD pipelines',
    category: 'eks-coreks',
    cardCount: 31,
    color: '#27AE60',
    icon: '🚀',
    cards: [
      { id: '1', front: `What are the two deployment methods covered in Module 04?`, back: `Manual deployment — using kubectl to apply manifests directly to the cluster. Automated deployment — using a CI/CD pipeline (build → test → push → deploy) that removes human error and enforces consistency.` },
      { id: '2', front: `Why do production teams prefer automated over manual deployments?`, back: `Automated pipelines are repeatable , auditable , and consistent — every deploy follows the same steps. Manual kubectl commands are fast but error-prone at scale and leave no automatic audit trail or rollback mechanism.` },
      { id: '3', front: `What are the 3 steps to scaling your deployment workflow?`, back: `1. Set up a container image repository (e.g. Amazon ECR) 2. Choose your package manager / deployment tools (e.g. Helm) 3. Automate your CI/CD pipeline (build → push → deploy on every commit)` },
      { id: '4', front: `What role does a container image repository play in the deployment workflow?`, back: `It is the central artifact store : your CI pipeline pushes built images there, your cluster pulls images from there. It enforces versioned, immutable artifacts so every environment gets exactly the same binary.` },
      { id: '5', front: `What is Amazon ECR in one sentence?`, back: `Amazon ECR is a fully managed registry for Docker images and OCI artifacts — highly available, scalable, encrypted at rest, with native IAM integration.` },
      { id: '6', front: `How many default private registries does an AWS account get in ECR?`, back: `One default private registry per AWS account (plus one default public registry). Within that registry you can create many repositories , each holding multiple image versions.` },
      { id: '7', front: `What is the relationship between an ECR registry and an ECR repository?`, back: `A registry is the top-level namespace (one per account). A repository is a named bucket inside the registry that stores images for one application. Multiple repositories can exist within a single registry.` },
      { id: '8', front: `Why does ECR not need separate Docker Hub–style credentials on AWS?`, back: `ECR uses IAM for authentication and authorization . On an EKS node or Fargate Pod, the attached IAM role grants pull access automatically. You authenticate Docker with a short-lived token: aws ecr get-login-password | docker login .` },
      { id: '9', front: `Does ECR support image signing?`, back: `Yes. ECR supports image signing via AWS Signer, which lets you cryptographically sign images so your cluster can verify that only approved, unmodified images are deployed. This integrates with OCI artifact signing standards.` },
      { id: '10', front: `What engine powers ECR Basic scanning?`, back: `Clair — an open-source static analysis tool for container image vulnerabilities. Basic scanning can be invoked on push or on demand, but is limited to once per 24 hours per image.` },
      { id: '11', front: `What additional capability does Enhanced scanning add over Basic?`, back: `Enhanced scanning (powered by Amazon Inspector) adds: • Language package scanning (npm, pip, gem — not just OS packages) • EventBridge events for automated remediation workflows • No 24-hour frequency limit Trade-off: extra cost via Amazon Inspector pricing.` },
      { id: '12', front: `What is the 24-hour rule for ECR Basic scanning?`, back: `Basic scanning can be triggered on push or on demand, but each image can only be scanned once per 24 hours . If you push multiple times within 24 hours, only the first push triggers a new scan. Enhanced scanning has no such limit.` },
      { id: '13', front: `What command authenticates Docker to Amazon ECR?`, back: `aws ecr get-login-password --region region | docker login --username AWS --password-stdin account .dkr.ecr. region .amazonaws.com This exchanges your IAM credentials for a short-lived Docker registry token.` },
      { id: '14', front: `How does an EKS node pull images from ECR without a stored password?`, back: `The node's IAM role (or Fargate Pod execution role) includes the ecr:GetAuthorizationToken and ecr:BatchGetImage permissions. The kubelet uses those permissions to automatically authenticate to ECR — no stored credentials needed.` },
      { id: '15', front: `What is the format of an ECR image URI?`, back: `account-id .dkr.ecr. region .amazonaws.com/ repository-name : tag Example: 123456789012.dkr.ecr.us-east-1.amazonaws.com/my-app:1.0.0` },
      { id: '16', front: `What is Helm and why do teams use it?`, back: `Helm is the Kubernetes package manager. Teams use it to: • Create standardised, reusable manifest templates • Eliminate deployment errors (no manual YAML editing per environment) • Manage app versioning (chart versions + app versions) • Perform in-place upgrades and rollbacks with a single command` },
      { id: '17', front: `What are the 4 common tasks Helm helps with?`, back: `1. Create standardised reusable templates 2. Eliminate deployment errors (no manual YAML edits) 3. Manage app versioning across environments 4. In-place upgrades (and helm rollback for quick recovery)` },
      { id: '18', front: `What is a Helm "release"?`, back: `A release is a specific deployed instance of a Helm chart in a Kubernetes cluster. The same chart can be installed multiple times (each is a separate release with its own name). Release name is used in templates via {{ .Release.Name }} .` },
      { id: '19', front: `KC 2 — Which file in a Helm chart is MANDATORY and contains chart metadata? A. pod.yaml B. values.yaml C. charts/ D. Chart.yaml`, back: `✓ D — Chart.yaml Chart.yaml is mandatory. It contains the chart's name, version, and description. values.yaml (B) is also mandatory but holds config values, not chart metadata. charts/ (C) is optional (dependencies).` },
      { id: '20', front: `What does Chart.yaml contain?`, back: `Chart.yaml is the chart's manifest . Key fields: • name — chart name • version — chart version (semver) • description — human-readable description • apiVersion — Helm API version (v2) • appVersion — the app version being deployed` },
      { id: '21', front: `What two special template variables does Helm inject automatically?`, back: `{{ .Release.Name }} — the name given at helm install time {{ .Release.Namespace }} — the target namespace All user values are under {{ .Values.* }} . Built-in chart metadata is under {{ .Chart.* }} .` },
      { id: '22', front: `What is the purpose of the charts/ directory?`, back: `The charts/ directory holds chart dependencies — other Helm charts that your chart depends on. These are declared in Chart.yaml under dependencies: and downloaded via helm dependency update . It is optional if you have no sub-charts.` },
      { id: '23', front: `How do you override values.yaml defaults at install time?`, back: `Two ways: 1. --set key=value flag on the command line (quick overrides) 2. -f custom-values.yaml to supply an entire override file This lets you use one chart across dev/staging/prod by supplying environment-specific values without changing templates.` },
      { id: '24', front: `What three sources can serve Helm charts according to this course?`, back: `1. Artifact Hub — public chart registry (artifacthub.io) 2. Amazon ECR — OCI artifact support; charts alongside images 3. Amazon S3 — private bucket as Helm repo; used in Lab 2` },
      { id: '25', front: `What does Lab 2 require you to do with S3 and Helm?`, back: `Lab 2 tasks: 1. Configure an S3 bucket as a Helm chart repository 2. Package and load your chart into S3 3. Deploy your application using helm install from S3 4. Review Chart.yaml and values.yaml` },
      { id: '26', front: `Why use ECR as a Helm chart repository instead of Artifact Hub?`, back: `Using ECR for Helm charts alongside container images means: • Single registry for both images and charts • IAM-controlled access — no separate credentials • Private by default — not publicly discoverable • OCI standard — push/pull with standard Docker tooling` },
      { id: '27', front: `KC 1 — Which two statements about Amazon ECR are true? (Select 2) A. Each account has one default private AND one default public registry B. Scanning requires Inspector on all repos C. Repos must be in the same region as the EKS cluster D. Multiple repositories can exist within a single registry`, back: `✓ A and D A : Each AWS account gets exactly one default private registry and one default public registry. D : Multiple repositories (one per app/service) can exist within a single registry. B is false (Inspector is optional). C is false (cross-region is supported).` },
      { id: '28', front: `KC 2 — Which Helm chart file is mandatory and contains chart metadata? A. pod.yaml B. values.yaml C. charts/ D. Chart.yaml`, back: `✓ D — Chart.yaml Chart.yaml is the ONLY mandatory file that contains chart metadata (name, version, description). values.yaml is also mandatory but holds configuration values, not chart metadata. charts/ is optional.` },
      { id: '29', front: `Exam trap: What is the difference between ECR Basic and Enhanced scanning triggers?`, back: `Basic : triggered on push OR on demand — but limited to once per 24 hours per image. Enhanced : continuous scanning (can run more than once per 24 hours) + emits EventBridge events for automated workflows. Extra cost.` },
      { id: '30', front: `Exam trap: What Helm template variable holds the name you give at install time?`, back: `{{ .Release.Name }} — set when you run helm install release-name ./chart . This is distinct from {{ .Chart.Name }} (the chart name from Chart.yaml) and {{ .Values.name }} (a user-defined value in values.yaml).` },
      { id: '31', front: `Which scanning engine scans OS and language packages?`, back: `Amazon Inspector (Enhanced scanning). It scans both OS-level packages (like Clair does for Basic) AND application language packages — npm, pip, Ruby gems, etc. This is critical for catching CVEs in application dependencies, not just OS libraries.` },
    ],
  },
  {
    id: 'eks-coreks-m05',
    title: 'EKS M05: Managing Apps at Scale',
    description: 'HPA, VPA, Cluster Autoscaler, Karpenter, GitOps, Argo CD',
    category: 'eks-coreks',
    cardCount: 38,
    color: '#9B59B6',
    icon: '📈',
    cards: [
      { id: '1', front: `What does HPA scale — pods or resources?`, back: `Pods (replicas). HPA scales the number of Pod instances horizontally — adding more identical pods under load, removing them when load drops. It does not resize CPU/memory on existing pods.` },
      { id: '2', front: `Write the HPA kubectl command for a deployment "myapp" targeting 50% CPU, min 3, max 10.`, back: `kubectl autoscale deployment myapp --cpu-percent=50 --min=3 --max=10 This creates an HPA object that watches average CPU utilisation across all pods and scales between 3 and 10 replicas to keep it near 50%.` },
      { id: '3', front: `What metrics can HPA use to make scaling decisions?`, back: `Built-in: CPU utilisation, memory utilisation Custom metrics: any metric from Kubernetes Metrics API (e.g. requests-per-second, queue depth) External metrics: metrics from outside the cluster (e.g. SQS queue length via KEDA)` },
      { id: '4', front: `What must pods have set for HPA CPU scaling to work?`, back: `Pods must have CPU resource requests set in their spec. HPA measures utilisation as a percentage of the requested CPU. Without requests, the Metrics Server cannot compute utilisation and HPA will not scale.` },
      { id: '5', front: `Which VPA component generates resource recommendations?`, back: `vpa-recommender. It continuously watches historical and current pod CPU/memory usage and produces a recommendation. The recommender writes its output to the VPA object's status.recommendation field.` },
      { id: '6', front: `Which VPA mode causes the least disruption to running workloads?`, back: `Off (zero disruption — read-only) or InPlaceOrRecreate (tries in-place resize first, only evicts if in-place is impossible). For production workloads, start with Off to observe recommendations before enabling any mutation.` },
      { id: '7', front: `What does vpa-admission-controller do?`, back: `It is a MutatingAdmissionWebhook that intercepts pod creation requests and rewrites the resource requests/limits to match VPA's current recommendation — before the pod is scheduled. This means new pods start right-sized even before the updater acts.` },
      { id: '8', front: `Why can't you combine VPA (CPU) and HPA (CPU) on the same Deployment?`, back: `They fight each other: HPA adds replicas when CPU is high; VPA simultaneously increases each pod's CPU request. The Metrics Server sees changing denominators, causing oscillation. Safe combo: HPA on custom/external metrics + VPA for CPU/memory right-sizing.` },
      { id: '9', front: `What triggers Cluster Autoscaler to add nodes?`, back: `Pods stuck in Pending state because no existing node has sufficient CPU/memory or matches the pod's node selector/affinity constraints. CA simulates scheduling and determines which node group to expand.` },
      { id: '10', front: `In the course's CA scale-out scenario (3 nodes, ASG max=6, then 14 replicas): what happens to the 2 pods after CA hits the ASG max?`, back: `They remain Pending indefinitely . CA cannot exceed the ASG's maximum . Once 6 nodes are running, CA has hit the hard limit — the 2 extra pods cannot be scheduled until you raise the ASG max or reduce the replica count.` },
      { id: '11', front: `What is MixedInstancesPolicy in the context of Cluster Autoscaler?`, back: `An ASG configuration that lets a single node group use a mix of On-Demand and Spot instances . CA can scale this group normally — it doesn't need to know which individual instance type is launched. Reduces cost while maintaining capacity.` },
      { id: '12', front: `What does the CA "auto-discovery" setup do?`, back: `CA tags ASGs with k8s.io/cluster-autoscaler/ cluster-name =owned and k8s.io/cluster-autoscaler/enabled=true . The CA controller discovers all matching ASGs automatically — no need to list ASG names statically in the CA deployment config.` },
      { id: '13', front: `What is the most fundamental difference between Karpenter and Cluster Autoscaler?`, back: `ASG dependency. CA requires pre-configured Auto Scaling Groups — it can only scale groups that exist. Karpenter provisions EC2 instances directly via the EC2 API, choosing instance type and size based on the pending pod's exact requirements — no ASG needed.` },
      { id: '14', front: `When should you choose Karpenter over Cluster Autoscaler?`, back: `Choose Karpenter when: • Spiky demand — fast node provisioning needed • Diverse compute — need many instance families/sizes • Cost optimisation — want Karpenter to auto-select cheapest instance that fits • Spot workloads — Karpenter handles spot interruption and rebalancing natively` },
      { id: '15', front: `What annotation prevents Karpenter from evicting a critical pod?`, back: `karpenter.sh/do-not-evict: "true" placed on the pod annotation. This tells Karpenter's consolidation logic to skip evicting that pod when it tries to remove underutilised nodes. Use for stateful workloads, long-running jobs, or anything that cannot tolerate rescheduling.` },
      { id: '16', front: `What are "layered constraints" in Karpenter?`, back: `Karpenter applies constraints from multiple layers: cluster-level NodePool constraints → workload pod spec (node selector, affinity, tolerations). Each layer narrows the provisioning decision. For example: the NodePool allows only us-east-1a + us-east-1b, and the pod's affinity further restricts to GPU nodes.` },
      { id: '17', front: `Who maintains Karpenter and what is its open-source status?`, back: `Karpenter is an open-source project originally created and currently maintained by AWS . It was donated to the CNCF ecosystem. It works with any Kubernetes cluster on any cloud, though it has native AWS EC2 integration via the karpenter-provider-aws .` },
      { id: '18', front: `Which two autoscalers operate at the node level (not pod level)?`, back: `Cluster Autoscaler and Karpenter both scale EC2 nodes. The difference: CA works through existing ASGs, Karpenter provisions nodes directly. HPA and VPA operate at the pod level.` },
      { id: '19', front: `Can HPA and Karpenter be used together? If so, how do they cooperate?`, back: `Yes — this is the recommended production pattern. HPA scales pods in response to traffic. When HPA adds pods that cannot fit on existing nodes, those pods go Pending. Karpenter detects Pending pods and provisions the right-sized node. They work at different layers and complement each other.` },
      { id: '20', front: `Which autoscaler has the most complex architecture with 3 separate deployments?`, back: `VPA — it needs vpa-recommender, vpa-updater, and vpa-admission-controller all running. HPA is built into the K8s controller manager. CA and Karpenter each deploy as a single controller.` },
      { id: '21', front: `Name the 8 stages of a CI/CD pipeline in order.`, back: `Plan → Code → Build → Test → Release → Deploy → Operate → Monitor Memory trick: "Please Can Bob Take Really Detailed Operations Metrics"` },
      { id: '22', front: `What is the difference between CI (Continuous Integration) and CD (Continuous Delivery)?`, back: `CI — automatically builds and tests code on every commit (Plan → Test stages). Ensures the codebase is always in a releasable state. CD — automatically delivers validated builds to a staging or production environment (Release → Deploy stages). May include a manual approval gate before production.` },
      { id: '23', front: `In an EKS CI/CD pipeline, where does the container image go after the Build stage?`, back: `The built and tested image is pushed to Amazon ECR (Elastic Container Registry). The Deploy stage then references the new ECR image tag in a Kubernetes Deployment manifest — either directly (imperative) or via a GitOps manifest update (declarative).` },
      { id: '24', front: `Walk through the GitOps app code change scenario step by step.`, back: `1. Developer pushes code to app Git repo 2. CI pipeline builds image, runs tests 3. CI pushes new image to ECR 4. CI updates image tag in manifest Git repo 5. Argo CD detects manifest change 6. Argo CD syncs → rolls out new pods in EKS cluster` },
      { id: '25', front: `What happens if someone runs "kubectl edit deployment myapp" directly on a GitOps-managed cluster?`, back: `Argo CD detects drift — it continuously compares live cluster state to Git. The direct kubectl change creates a mismatch. On the next sync cycle (or immediately if auto-sync is on), Argo CD reverts the cluster to the state declared in Git , undoing the manual change.` },
      { id: '26', front: `Why is GitOps described as "declarative" rather than "imperative"?`, back: `Declarative = you describe the desired end state (e.g. "3 replicas of v2.1.0"). The GitOps controller figures out how to get there. Imperative = you issue commands (e.g. "kubectl set image", "kubectl scale"). GitOps eliminates one-off imperative commands because they bypass Git and get reverted.` },
      { id: '27', front: `What are the two Git repositories in a GitOps setup and what does each contain?`, back: `App code repo — application source code. Changes here trigger the CI pipeline. Manifest repo (infra/config repo) — Kubernetes YAML manifests (Deployments, Services, etc.) + infrastructure-as-code. Changes here are watched by Argo CD and applied to the cluster.` },
      { id: '28', front: `How does Argo CD enforce the GitOps "approved changes applied automatically" principle?`, back: `When auto-sync is enabled in Argo CD, any merge to the watched Git branch automatically triggers a sync to the cluster — no human operator needs to run kubectl or approve a deploy. The Git merge/pull request process IS the approval gate. Argo CD then ensures the cluster matches.` },
      { id: '29', front: `KC — You have a stateless web app with unpredictable traffic spikes. Which autoscaler should you use first? A. VPA B. HPA C. Karpenter only D. CA only`, back: `✓ B — HPA HPA scales pod replicas in response to CPU/traffic. For spiky traffic on stateless apps, HPA is the primary tool. Karpenter/CA add nodes when HPA can't fit the extra pods.` },
      { id: '30', front: `KC — Which VPA component actually evicts pods that need updated resources? A. vpa-recommender B. vpa-admission-controller C. vpa-updater`, back: `✓ C — vpa-updater vpa-recommender generates recommendations. vpa-admission-controller injects resources at pod creation. vpa-updater is the only one that evicts running pods to apply new resource settings.` },
      { id: '31', front: `KC — Cluster Autoscaler cannot add more nodes. What is the most likely cause?`, back: `The Auto Scaling Group's maximum size has been reached. CA cannot scale beyond the ASG max. Fix: increase the max capacity on the ASG directly. Also check: IAM permissions for CA to modify the ASG, and that the CA pod is running and healthy.` },
      { id: '32', front: `KC — In GitOps, a developer directly edits a running pod using kubectl. What happens next?`, back: `Argo CD detects the drift between live cluster state and the Git manifest. On the next sync cycle (or immediately with auto-sync), it reverts the cluster to the Git-declared state . The manual change is overwritten.` },
      { id: '33', front: `Quick-fire: What does the VPA "Off" mode do?`, back: `Generates recommendations only — no changes are applied to pods. Use it to observe what VPA would recommend before enabling mutations. Zero risk to running workloads.` },
      { id: '34', front: `Quick-fire: What annotation prevents Karpenter from removing a node with a critical pod?`, back: `karpenter.sh/do-not-evict: "true" on the pod. This signals Karpenter's consolidation loop to leave the node running even if it's underutilised.` },
      { id: '35', front: `Quick-fire: Name the 4 GitOps principles.`, back: `1. Git as single source of truth 2. Declarative (not imperative) 3. Approved changes applied automatically 4. Software agents ensure consistent state (drift detection + auto-reconcile)` },
      { id: '36', front: `Quick-fire: Which autoscaler does NOT use Auto Scaling Groups?`, back: `Karpenter. It provisions EC2 instances directly via the EC2 Fleet API, choosing the right instance type based on pending pod requirements. Cluster Autoscaler requires pre-configured ASGs.` },
      { id: '37', front: `Quick-fire: What are the 3 deployments that make up VPA?`, back: `1. vpa-recommender — generates resource recommendations 2. vpa-updater — evicts pods needing resource changes 3. vpa-admission-controller — mutates new pods at creation` },
      { id: '38', front: `Quick-fire: In the GitOps Argo CD flow, what triggers Argo CD to sync the cluster?`, back: `A change in the manifest Git repo . Argo CD polls the repo (or uses webhooks) and detects that the desired state (Git) differs from the current cluster state. It then applies the diff to bring the cluster in line with Git.` },
    ],
  },
  {
    id: 'eks-coreks-m06',
    title: 'EKS M06: Managing Networking',
    description: 'VPC CNI, pod networking, service types, Ingress, NetworkPolicy',
    category: 'eks-coreks',
    cardCount: 43,
    color: '#818cf8',
    icon: '🌐',
    cards: [
      { id: '1', front: `What is the difference between a public and private subnet in AWS?`, back: `A public subnet has a route to an Internet Gateway (IGW), so resources can receive inbound traffic from the internet. A private subnet has no such route — outbound internet access uses a NAT Gateway, but no inbound connections are possible directly.` },
      { id: '2', front: `What makes security groups "stateful"?`, back: `Stateful means return traffic is automatically allowed . If you allow inbound HTTP (port 80), the response traffic is permitted without an explicit outbound rule. NACLs, by contrast, are stateless — you must explicitly allow both directions.` },
      { id: '3', front: `What is an ENI and why does it matter for EKS?`, back: `An Elastic Network Interface (ENI) is a virtual network card you can attach to EC2 instances. In EKS, the VPC CNI plugin assigns secondary IP addresses from ENIs attached to nodes — giving each pod its own real VPC IP without needing an overlay network.` },
      { id: '4', front: `How do two containers in the same pod communicate?`, back: `Containers in the same pod share a network namespace . They see the same network interfaces and communicate over localhost (127.0.0.1). Port conflicts between containers in the same pod are possible — each must use a unique port.` },
      { id: '5', front: `What mechanism handles pod-to-pod traffic on the same node (intrahost)?`, back: `Linux Virtual Ethernet Device (veth) pairs. Each pod gets a veth pair — one end in the pod's network namespace, the other in the host namespace. The host's Linux bridge connects veth pairs so pods on the same node can reach each other directly.` },
      { id: '6', front: `What mechanism handles pod-to-pod traffic across different nodes (interhost)?`, back: `Amazon VPC CNI plugin. Because each pod gets a real VPC IP address (not an overlay address), pods on different nodes communicate directly via standard VPC routing — no encapsulation or tunnelling required. The VPC subnet's route table handles it natively.` },
      { id: '7', front: `How does external traffic reach a workload inside an EKS cluster?`, back: `Via a LoadBalancer service — which provisions an AWS NLB or ALB. The load balancer sits outside the cluster, receives external traffic, and forwards it to the correct node/pod. NodePort and Ingress are alternatives for different use cases.` },
      { id: '8', front: `What is the core difference VPC CNI brings vs a traditional overlay CNI?`, back: `With VPC CNI, each pod gets an IP address directly from the VPC CIDR — the same IP is visible both inside the pod and on the VPC network. Traditional overlay CNIs (Calico, Flannel) give pods overlay IPs and encapsulate traffic, adding overhead. VPC CNI requires no encapsulation.` },
      { id: '9', front: `How does the VPC CNI plugin assign IP addresses to pods?`, back: `The VPC CNI attaches secondary ENIs to the EC2 node and pre-warms a pool of secondary IP addresses from the VPC subnet. When a pod is scheduled, the CNI assigns one of these pre-allocated IPs to the pod's veth interface — startup is fast because IPs are already reserved.` },
      { id: '10', front: `What limits the number of pods a node can run in EKS with VPC CNI?`, back: `The maximum number of ENIs and secondary IPs the EC2 instance type supports. Larger instances (e.g. m5.large vs m5.xlarge) support more ENIs and more secondary IPs, thus more pods. This is an important sizing constraint when choosing instance types for EKS node groups.` },
      { id: '11', front: `What is "IP mode" vs "instance mode" in the context of the VPC CNI?`, back: `Instance mode: the load balancer (NLB) sends traffic to the node's port; kube-proxy then routes it to the pod. IP mode: the NLB sends traffic directly to the pod IP , bypassing kube-proxy entirely. IP mode requires the VPC CNI and reduces latency.` },
      { id: '12', front: `What is the default network behaviour in a Kubernetes cluster with no NetworkPolicies?`, back: `All pods can communicate with all other pods in any namespace, and all pods can initiate external connections. There is no built-in isolation — you must explicitly create NetworkPolicy resources to restrict traffic.` },
      { id: '13', front: `What does a NetworkPolicy with an empty podSelector ({}) select?`, back: `An empty podSelector: {} matches ALL pods in the namespace . Combined with policyTypes: [Ingress, Egress] and no rules, this creates a default deny all policy — blocking all inbound and outbound traffic for every pod in the namespace.` },
      { id: '14', front: `Do NetworkPolicies apply to pods across namespaces?`, back: `NetworkPolicies are namespace-scoped . A policy in namespace A only affects pods in namespace A. To allow cross-namespace traffic, use a namespaceSelector in the ingress/egress rules. Without it, only pods within the same namespace are matched.` },
      { id: '15', front: `Does EKS enforce NetworkPolicies by default?`, back: `NetworkPolicy enforcement depends on the CNI plugin , not Kubernetes itself. The Amazon VPC CNI supports NetworkPolicy enforcement through the Network Policy Controller add-on . Without a CNI that supports it, NetworkPolicy objects are accepted by the API server but have no effect .` },
      { id: '16', front: `NetworkPolicies operate at which layer — pod, node, or namespace?`, back: `Pod level. NetworkPolicies select pods using label selectors and control traffic to/from those specific pods. They are namespace-scoped objects but apply at the individual pod (IP/port) level — not at the node or subnet level. Security groups operate at a higher (node/ENI) level.` },
      { id: '17', front: `What problem does "security groups for pods" solve?`, back: `Without this feature, all pods on a node share the node's security group. You cannot restrict which pods can reach an RDS database at the SG level. Security groups for pods assign an AWS SG to a specific pod, enabling precise VPC-level access control (e.g. only the api pod can reach the RDS SG).` },
      { id: '18', front: `How are security groups assigned to pods in EKS?`, back: `Using a custom EKS CRD called SecurityGroupPolicy . You define a SecurityGroupPolicy resource specifying a podSelector (or serviceAccountSelector ) and the security group IDs to assign. The VPC CNI plugin then attaches a branch ENI with those SGs to matching pods.` },
      { id: '19', front: `What is the key difference between NetworkPolicy and security groups for pods?`, back: `NetworkPolicy is a Kubernetes-native, CNI-enforced control for pod-to-pod traffic using label selectors. Security groups for pods are AWS-native VPC controls that apply to pod ENIs — useful for controlling access to AWS services (RDS, ElastiCache) and enforcing VPC-level rules alongside K8s-level policies.` },
      { id: '20', front: `What is a ClusterIP service and who can reach it?`, back: `ClusterIP is the default service type. It assigns a stable virtual IP address reachable only from within the cluster . Pods use this IP (or the service DNS name) to reach other pods. External clients cannot reach a ClusterIP service — it is completely internal.` },
      { id: '21', front: `What port range does NodePort use and how does traffic flow?`, back: `NodePort allocates a port in the range 30000–32767 on every node. External clients send traffic to NodeIP : NodePort ; kube-proxy on the node forwards it to the ClusterIP, which routes to a pod. NodePort wraps ClusterIP — a ClusterIP is created automatically.` },
      { id: '22', front: `What does a LoadBalancer service provision in AWS?`, back: `A LoadBalancer service automatically provisions an AWS Network Load Balancer (NLB) by default, or an ALB if the AWS Load Balancer Controller is installed and annotated. It wraps NodePort — traffic hits the LB, goes to a node port, and kube-proxy routes it to a pod. Annotation: service.beta.kubernetes.io/aws-load-balancer-nlb-target-type: instance` },
      { id: '23', front: `How does ExternalName differ from all other service types?`, back: `ExternalName creates a CNAME DNS record that maps the service name to an external DNS hostname (e.g. my.db.example.com ). There is no proxying , no ClusterIP, and no kube-proxy involvement. It lets pods use a Kubernetes service name while the actual endpoint lives outside the cluster.` },
      { id: '24', front: `What is the nesting hierarchy of service types?`, back: `LoadBalancer wraps NodePort, which wraps ClusterIP. Creating a LoadBalancer service automatically creates a NodePort and a ClusterIP. Traffic path: Internet → AWS LB → Node:NodePort → kube-proxy → ClusterIP → Pod. ExternalName is separate — no ClusterIP, just a DNS CNAME.` },
      { id: '25', front: `What is the difference between LoadBalancer instance mode and IP mode?`, back: `Instance mode: NLB routes traffic to the EC2 node's port; kube-proxy routes to the pod. IP mode: NLB routes directly to the pod IP — kube-proxy is bypassed. IP mode requires VPC CNI (pods need real VPC IPs). IP mode reduces hops and latency.` },
      { id: '26', front: `What does the AWS Load Balancer Controller provision for an Ingress resource?`, back: `An Application Load Balancer (ALB) . When you create an Ingress with ingressClassName: alb , the AWS LBC creates an ALB with listener rules matching the Ingress path/host rules. Each backend service becomes a target group on the ALB.` },
      { id: '27', front: `What annotation triggers the AWS LBC to manage a LoadBalancer service with an NLB?`, back: `service.beta.kubernetes.io/aws-load-balancer-nlb-target-type: instance (or ip ). The presence of the AWS LBC in the cluster and this annotation causes it to provision an NLB instead of using the in-tree ELB provisioner. The ip value enables direct pod IP targeting.` },
      { id: '28', front: `Why would you install the AWS Load Balancer Controller instead of relying on the built-in cloud provider?`, back: `The built-in in-tree cloud provider only supports Classic ELBs and basic NLBs. AWS LBC gives you: ALB support for Ingress , IP mode targets (pod-level routing), WAF integration, SSL policies, HTTP/2, path-based routing, multiple certificates, and faster feature updates from AWS.` },
      { id: '29', front: `What is the main advantage of Ingress over multiple LoadBalancer services?`, back: `A single Ingress (one ALB) can route to multiple backend services based on URL path or hostname — instead of provisioning one NLB per service. Example: /webapp1 → webapp1:32003, /webapp2 → webapp2:32004. This saves cost and reduces complexity.` },
      { id: '30', front: `Does Ingress work without an Ingress Controller?`, back: `No. An Ingress object alone does nothing — it is just a configuration spec. An Ingress Controller watches for Ingress objects and configures the actual load balancer. In EKS, the AWS Load Balancer Controller is the standard Ingress Controller and creates an ALB.` },
      { id: '31', front: `How do you tell Kubernetes which Ingress Controller should handle an Ingress object?`, back: `Set spec.ingressClassName: alb in the Ingress manifest (or the older annotation kubernetes.io/ingress.class: "alb" ). This tells the AWS Load Balancer Controller to claim and act on this Ingress, creating a corresponding ALB with matching listener rules.` },
      { id: '32', front: `What type of routing does Ingress support?`, back: `Two types: Path-based routing — route based on URL path prefix (e.g. /api → api-service). Host-based routing — route based on HTTP Host header (e.g. api.example.com → api-service, app.example.com → frontend-service). Both can be combined in one Ingress.` },
      { id: '33', front: `What is CoreDNS and what does it resolve?`, back: `CoreDNS is the cluster DNS add-on that runs as a Deployment in the kube-system namespace. It resolves Kubernetes internal names: services ( svc.cluster.local ), pods, and namespaced resources. It is the first DNS resolver a pod hits for any query.` },
      { id: '34', front: `What is the DNS format for a Kubernetes service?`, back: `Full form: service-name . namespace .svc.cluster.local . Within the same namespace, pods can use just service-name . CoreDNS appends the search domain. Example: backend (same namespace) or backend.prod.svc.cluster.local (cross-namespace).` },
      { id: '35', front: `A pod queries an RDS endpoint. Which DNS layer resolves it?`, back: `Layer 2 — Route 53 VPC Resolver. CoreDNS (layer 1) forwards queries it cannot resolve to the VPC's default DNS resolver (169.254.169.253). RDS endpoints are VPC-local DNS names that Route 53 resolves within the VPC. They don't need an upstream public DNS server.` },
      { id: '36', front: `How does CoreDNS know when to forward a query to the Route 53 resolver?`, back: `CoreDNS has a forward plugin in its ConfigMap. Any query that doesn't match cluster.local or other cluster domains is forwarded to the upstream resolver — which is the VPC's DNS server at the VPC base address +2 (e.g. 10.0.0.2). That resolver then either answers (VPC resource) or recurses to the internet.` },
      { id: '37', front: `KC1 — Which service type can be accessed from outside the cluster? A. ClusterIP B. ExternalName C. NodePort only D. Both NodePort and LoadBalancer`, back: `✓ D — Both NodePort and LoadBalancer NodePort exposes the service on a static port on every node IP (external access via NodeIP:NodePort). LoadBalancer additionally provisions an AWS LB. ClusterIP (A) is internal only. ExternalName (B) maps to external DNS but is not itself externally accessible.` },
      { id: '38', front: `KC2 — What is the Amazon VPC CNI plugin? A. A security tool for pods B. A monitoring agent C. A routing overlay D. Plugin allowing pods to have the same IP inside the pod as on the VPC network`, back: `✓ D — Plugin allowing pods to have same IP inside the pod as on the VPC network This eliminates the need for an overlay network. Pods get IPs from the VPC CIDR — the pod IP is the same address visible to other VPC resources. Option C (routing overlay) is the opposite of what VPC CNI does.` },
      { id: '39', front: `Traffic routing activity — Intranode pod-to-pod uses what mechanism?`, back: `Linux Virtual Ethernet Device (veth) pairs. Each pod on the node has a veth pair — one end in the pod namespace, the other in the host namespace. Intra-node pods communicate via the host network bridge connecting all veth pairs on that node.` },
      { id: '40', front: `Traffic routing activity — Internode pod-to-pod (different nodes) uses what?`, back: `Amazon VPC CNI plugin. Because each pod has a real VPC IP, the VPC routing table routes packets directly between pods on different nodes — no tunnel or encapsulation. This is the key differentiator of VPC CNI versus overlay-based CNIs.` },
      { id: '41', front: `Traffic routing activity — External traffic to a service uses what?`, back: `LoadBalancer service. An AWS NLB or ALB is provisioned by the service controller (or AWS LBC). External clients connect to the load balancer's public endpoint; the LB forwards traffic to the appropriate node port or pod IP.` },
      { id: '42', front: `Which Ingress Controller does the AWS Load Balancer Controller create — NLB or ALB?`, back: `ALB for Ingress resources, NLB for LoadBalancer services. The AWS LBC handles both. An Ingress with ingressClassName: alb creates an Application Load Balancer (Layer 7, HTTP/HTTPS). A LoadBalancer service with the NLB annotation creates a Network Load Balancer (Layer 4, TCP/UDP).` },
      { id: '43', front: `Name the three CoreDNS / DNS layers in EKS in order.`, back: `1. CoreDNS — resolves resources inside the EKS cluster ( *.cluster.local ) 2. Route 53 VPC Resolver — resolves resources inside the VPC (RDS endpoints, EC2 names) 3. Upstream DNS — resolves resources outside the VPC (public internet hostnames)` },
    ],
  },
  {
    id: 'eks-coreks-m07',
    title: 'EKS M07: Configuring Observability',
    description: 'Metrics, logs, traces · Prometheus, Fluent Bit, X-Ray, ADOT',
    category: 'eks-coreks',
    cardCount: 39,
    color: '#1ABC9C',
    icon: '🔭',
    cards: [
      { id: '1', front: `What is the course definition of observability?`, back: `"A measure of how well we can understand a system from the work it does, and how to make it better." Observability is not just monitoring — it is the ability to ask arbitrary questions about system state and get answers.` },
      { id: '2', front: `Name the 3 pillars of observability.`, back: `1. Metrics — numerical measurements over time (CPU, memory, request rates) 2. Logs — discrete events (text output from containers and services) 3. Traces — end-to-end request flow across multiple services` },
      { id: '3', front: `What are the five values of observability insight?`, back: `1. Customer experience — know when users are impacted 2. Performance and cost — optimise resource use 3. Trends — detect slow degradation over time 4. Troubleshooting — diagnose incidents faster 5. Learning and improvement — understand system behaviour` },
      { id: '4', front: `Which pillar answers "what happened at 14:32:07 on pod web-abc"?`, back: `Logs — logs are discrete timestamped events. You search logs to find the exact error message, stack trace, or state change at a specific moment in a specific container.` },
      { id: '5', front: `Which pillar answers "which downstream service is causing slow checkout?"`, back: `Traces — distributed tracing follows a single request across all services and records latency per segment. You can see that checkout calls payment-service which takes 800 ms, vs. 5 ms for everything else.` },
      { id: '6', front: `Name four reasons containers make observability harder than VMs.`, back: `1. Microservice complexity — many services interacting unpredictably 2. Metric volume — hundreds of pods each emit many metrics 3. Transient containers — container stops → evidence disappears 4. OS is just one factor — container behaviour also shaped by orchestration, networking, sidecars` },
      { id: '7', front: `Why are transient containers a problem for log analysis?`, back: `When a container crashes and restarts, any logs stored inside the container are lost. You must use an external log forwarder (like Fluent Bit ) to stream logs off-node to durable storage before the container terminates — otherwise the evidence disappears with it.` },
      { id: '8', front: `Why doesn't traditional OS-level monitoring scale for microservices?`, back: `Traditional monitoring watches a handful of servers. A microservice cluster may have hundreds of pods , each emitting CPU, memory, network, and custom application metrics. Polling each pod individually is impractical — you need a system (like Prometheus ) designed to scrape thousands of endpoints at scale.` },
      { id: '9', front: `What is the Prometheus metrics text format?`, back: `metric_name{"tag"="value"} value Example: cpu_usage{pod="web-abc",ns="prod"} 0.45 It is plain text served at an HTTP /metrics endpoint. Prometheus polls these endpoints on a schedule and stores the time-series data.` },
      { id: '10', front: `What is PromQL?`, back: `PromQL (Prometheus Query Language) is the query language used to select, filter, and aggregate Prometheus time-series data. Examples: rate(http_requests_total[5m]) for request rate, or sum by (pod) (container_memory_usage_bytes) for per-pod memory.` },
      { id: '11', front: `How does Prometheus collect metrics from the Kubernetes control plane?`, back: `The same way it scrapes pods: via an HTTP /metrics endpoint. You can access control plane metrics directly with kubectl get --raw /metrics . Prometheus is configured with a scrape job targeting the API server, scheduler, and controller-manager endpoints.` },
      { id: '12', front: `What role does Grafana play alongside Prometheus?`, back: `Grafana is a dashboarding tool that queries Prometheus (via PromQL) and renders time-series data as graphs, gauges, and heatmaps. Prometheus stores and queries data; Grafana visualises it. They are often deployed together in a cluster using the kube-prometheus-stack Helm chart.` },
      { id: '13', front: `How does Prometheus discover which pods to scrape?`, back: `Prometheus uses Kubernetes service discovery — it queries the K8s API to find pods, services, and endpoints. Pods annotated with prometheus.io/scrape: "true" are automatically included. ServiceMonitor CRDs (from the Prometheus Operator) provide a declarative way to configure scrape targets.` },
      { id: '14', front: `What does CloudWatch Container Insights collect?`, back: `Metrics: CPU, memory, disk, network, container data Logs: container stdout/stderr Aggregated at: cluster / node / pod / task / service level` },
      { id: '15', front: `How is CloudWatch Container Insights installed on EKS nodes?`, back: `As a DaemonSet agent installed on each node. The CloudWatch Agent (or the newer Container Insights for EKS add-on) runs one pod per node, collects local metrics and logs, and ships them to CloudWatch. It requires appropriate IAM permissions on the node role.` },
      { id: '16', front: `What are the aggregation levels supported by Container Insights?`, back: `Container Insights aggregates metrics at five levels: Cluster → Node → Pod → Task → Service This lets you drill from "why is the cluster CPU high?" all the way down to a specific container in a specific pod.` },
      { id: '17', front: `Container Insights vs Prometheus — when would you choose each?`, back: `Container Insights: simpler setup, AWS-native, integrates with CloudWatch alarms and Logs Insights. Good for teams already in the CloudWatch ecosystem. Prometheus: richer query language (PromQL), community ecosystem, custom app metrics, works across multiple clouds. Requires more operational overhead.` },
      { id: '18', front: `Name the 5 EKS control plane log types.`, back: `1. API server 2. Audit 3. Authenticator 4. Controller manager 5. Scheduler Each must be individually enabled in the EKS Console (Logging tab) or via the API. They are sent to CloudWatch Logs.` },
      { id: '19', front: `How are application container logs collected in EKS?`, back: `Containers write logs to stdout/stderr. The container runtime writes these to the node's filesystem . A DaemonSet agent (typically Fluent Bit) runs on each node, tails those log files, and ships them to a configured destination. This ensures logs survive container restarts.` },
      { id: '20', front: `Where do EKS control plane logs go once enabled?`, back: `Amazon CloudWatch Logs . Each control plane log type gets its own log group: /aws/eks/ cluster-name /cluster . From CloudWatch, you can use Logs Insights for querying, set metric filters, or export to S3 for long-term retention.` },
      { id: '21', front: `What are the three stages of the log routing workflow?`, back: `1. Log collection and forwarding — agent on node reads and forwards logs 2. Log aggregation — logs from all nodes combined in one store 3. Log analysis — search, filter, visualise (e.g. OpenSearch Dashboards)` },
      { id: '22', front: `What is Fluent Bit and how is it deployed in EKS?`, back: `Fluent Bit is a lightweight, high-performance log forwarder. In EKS it is deployed as a DaemonSet — one pod per node. It tails container log files from the node filesystem and routes them to configured output plugins (OpenSearch, S3, CloudWatch, Kinesis Firehose).` },
      { id: '23', front: `How does Fargate log routing differ from EC2 node log routing?`, back: `On EC2, Fluent Bit runs as a DaemonSet you deploy. On Fargate , there are no nodes to deploy DaemonSets on. Instead, Fargate reads log routing config from a ConfigMap named aws-logging in a namespace called aws-observability (which must be labelled aws-observability: enabled ).` },
      { id: '24', front: `What namespace and ConfigMap name are required for Fargate log routing?`, back: `Namespace: aws-observability (must have label aws-observability: enabled ) ConfigMap name: aws-logging Contains an output.conf key with Fluent Bit output plugin config (e.g. data_firehose ).` },
      { id: '25', front: `In the Fargate log routing ConfigMap, what OUTPUT plugin sends logs to Kinesis Data Firehose?`, back: `[OUTPUT] Name data_firehose Match * region us-west-2 delivery_stream my-stream-firehose The plugin name is data_firehose . You specify the target delivery stream and region.` },
      { id: '26', front: `What are the four Fluent Bit log routing destinations covered in the course?`, back: `1. Amazon OpenSearch — full-text search and dashboards 2. Amazon S3 — archival storage 3. CloudWatch Logs — AWS-native monitoring 4. Kinesis Data Firehose — streaming to multiple downstream targets` },
      { id: '27', front: `Why is traditional debugging insufficient for microservices?`, back: `Traditional debugging (inspecting a single service) doesn't scale because a request may touch dozens of services . You cannot tell which service is slow, which failed, or how errors propagate, by looking at one service's logs in isolation. Distributed tracing provides the full picture across all services.` },
      { id: '28', front: `What is AWS Distro for OpenTelemetry (ADOT)?`, back: `ADOT is AWS's distribution of the OpenTelemetry Collector. It runs as a sidecar or DaemonSet in your cluster, collects trace spans (and metrics) from instrumented applications, and forwards them to AWS X-Ray (and other backends). It is vendor-neutral: OpenTelemetry SDKs work with many backends.` },
      { id: '29', front: `What four insights does distributed tracing provide that logs cannot?`, back: `1. Service discovery — automatically maps which services exist and talk to each other 2. Individual operation insights — latency per operation within a service 3. Service-isolated issues — pinpoint exactly which service is the bottleneck 4. Root-cause analysis — trace the error back to its origin across the call chain` },
      { id: '30', front: `What does AWS X-Ray show you?`, back: `X-Ray provides: • A service map — visual graph of all services and their connections • Trace timeline — Gantt-style view of latency per segment in a single request • Bottleneck identification — which segment contributes most latency • Error rates per service segment` },
      { id: '31', front: `What is the full observability tooling stack summarised at module end?`, back: `Metrics: Prometheus + Grafana or CloudWatch Container Insights Logs: Fluent Bit → OpenSearch / S3 / CloudWatch / Kinesis Traces: ADOT → AWS X-Ray For Fargate logs: aws-observability namespace ConfigMap` },
      { id: '32', front: `KC 1 — Which tool scrapes /metrics endpoints and stores time-series data? A. Grafana B. Prometheus C. X-Ray D. Fluent Bit`, back: `✓ B — Prometheus Prometheus scrapes /metrics endpoints. Grafana (A) visualises Prometheus data. X-Ray (C) handles traces. Fluent Bit (D) forwards logs.` },
      { id: '33', front: `KC 2 — What command exposes control plane metrics directly? A. kubectl describe metrics B. kubectl get --raw /metrics C. aws eks get-metrics`, back: `✓ B — kubectl get --raw /metrics The --raw flag bypasses the kubectl object model and returns the raw HTTP response from the API server — in this case, the Prometheus-format metrics endpoint.` },
      { id: '34', front: `KC 3 — Fargate log routing is configured in which namespace?`, back: `aws-observability — the namespace must exist and be labelled aws-observability: enabled . Inside it, a ConfigMap named aws-logging contains the Fluent Bit output configuration.` },
      { id: '35', front: `KC 4 — Which AWS service visualises distributed traces sent by ADOT?`, back: `AWS X-Ray . ADOT (AWS Distro for OpenTelemetry) is the collector that gathers spans from your instrumented services. X-Ray is the storage and visualisation backend that shows you the service map and per-segment latency.` },
      { id: '36', front: `KC 5 — EKS control plane logs are enabled per-type in which console location?`, back: `EKS Console → your cluster → Logging tab . You toggle each of the 5 log types (API server, Audit, Authenticator, Controller manager, Scheduler) individually. Once enabled, they stream to CloudWatch Logs under /aws/eks/ cluster /cluster .` },
      { id: '37', front: `KC 6 — Lab 4 routes container logs through which services in order?`, back: `Fluent Bit DaemonSet → Kinesis Data Firehose → Amazon S3 Lab 4 also configures OpenSearch for log search, and uses Prometheus + Grafana for metrics on the same cluster.` },
      { id: '38', front: `KC 7 — Which log type records "who did what, when" in the Kubernetes API?`, back: `Audit logs — they record every request to the Kubernetes API server including the caller's identity, the resource accessed, and the action performed. Essential for security forensics and compliance.` },
      { id: '39', front: `KC 8 — CloudWatch Container Insights aggregates at which 5 levels?`, back: `Cluster → Node → Pod → Task → Service This hierarchy lets you start at the cluster level and drill down to a single container to find the source of a performance issue.` },
    ],
  },
  {
    id: 'eks-coreks-m08',
    title: 'EKS M08: Managing Storage',
    description: 'StatefulSets, PV/PVC, EBS CSI, EFS CSI, ASCP, IRSA',
    category: 'eks-coreks',
    cardCount: 38,
    color: '#E67E22',
    icon: '💾',
    cards: [
      { id: '1', front: `What are the three stateful requirements the course identifies?`, back: `1. Consistent access per instance — each pod accesses its own data, not shared data 2. Data persistence independent of container/node — data survives crashes and rescheduling 3. Storage suited for app design — block vs file vs object depending on the workload` },
      { id: '2', front: `Can you use ephemeral storage (emptyDir) for a stateful database pod?`, back: `No. An emptyDir volume is created when the pod is scheduled and deleted when the pod is removed — even if the pod restarts. All database data would be lost. Use a PersistentVolumeClaim backed by EBS or EFS instead.` },
      { id: '3', front: `What kind of storage is appropriate for a scratch/cache volume that doesn't need to persist?`, back: `Ephemeral volume (emptyDir) . Declared in the pod spec under volumes: with emptyDir: {} . It is fast (can be backed by memory with medium: Memory ), temporary, and requires no provisioning — perfect for caching or inter-container scratch space.` },
      { id: '4', front: `Name four use cases that call for a StatefulSet over a Deployment.`, back: `1. Stable network IDs — pod DNS name must be predictable (e.g., mysql-0.mysql-svc) 2. Persistent storage per pod — each replica owns its data 3. Ordered deployment/scaling — pod N must be Running before pod N+1 starts 4. Ordered rolling updates — updates proceed in reverse ordinal order` },
      { id: '5', front: `What happens to a StatefulSet pod's PVC when the pod is deleted?`, back: `The PVC is retained — it is NOT deleted with the pod. When the pod is rescheduled (same ordinal), Kubernetes re-attaches the same PVC. This guarantees data continuity across reschedules and node failures.` },
      { id: '6', front: `How does a StatefulSet pod name differ from a Deployment pod name?`, back: `StatefulSet: name - ordinal — e.g., mysql-0 , mysql-1 . The ordinal is stable; if mysql-1 is killed, its replacement is also named mysql-1 . Deployment: name - replicaset-hash - random — no stable identity.` },
      { id: '7', front: `What is the difference between a PV and a PVC?`, back: `PV (PersistentVolume) is a cluster-level resource that represents actual storage — an EBS volume, EFS mount point, etc. It is provisioned by an admin or dynamically. PVC (PersistentVolumeClaim) is a request for storage by a user — specifying size and access mode. Kubernetes binds the PVC to a suitable PV automatically.` },
      { id: '8', front: `What does volumeBindingMode: WaitForFirstConsumer do?`, back: `It tells Kubernetes to delay PV creation until a pod using the PVC is actually scheduled to a node. This ensures the EBS volume is created in the same Availability Zone as the pod — without this, the volume might be created in a different AZ, causing the pod to be unable to mount it.` },
      { id: '9', front: `Which object should an app developer reference in a pod manifest — PV or PVC?`, back: `PVC only. Developers use PVCs in pod specs ( volumes[].persistentVolumeClaim.claimName ). PVs are an admin/infrastructure concern. This separation of concerns means developers don't need to know the underlying storage type or backend.` },
      { id: '10', front: `What are the three access modes for a PersistentVolume?`, back: `ReadWriteOnce (RWO) — one node mounts read-write (EBS) ReadOnlyMany (ROX) — many nodes mount read-only ReadWriteMany (RWX) — many nodes mount read-write (EFS) Note: EBS only supports RWO. EFS supports RWX.` },
      { id: '11', front: `What does allowVolumeExpansion: true in a StorageClass enable?`, back: `It allows users to resize an existing PVC by editing its spec.resources.requests.storage to a larger value. The EBS CSI driver will expand the underlying EBS volume without needing to delete and recreate the PVC or the pod.` },
      { id: '12', front: `You have a Fargate pod that needs persistent storage. Which AWS service can you use?`, back: `Amazon EFS only. EBS is not supported on Fargate pods — EBS requires attaching to an EC2 instance. EFS uses NFS over the network and is fully compatible with Fargate. You must use the EFS CSI driver and mount an EFS access point.` },
      { id: '13', front: `What is the default recommended EBS volume type for EKS StorageClasses?`, back: `gp3 — the newest general-purpose SSD type. It provides better baseline performance than gp2 and lets you provision IOPS and throughput independently. AWS recommends gp3 as the default in new StorageClass configurations ( parameters.type: gp3 ).` },
      { id: '14', front: `Multiple replicas of a machine-learning training job need to read the same dataset simultaneously. Which storage?`, back: `Amazon EFS — because it supports ReadWriteMany , allowing multiple pods across multiple nodes to mount and read the same file system concurrently. EBS (RWO) can only be mounted by one node at a time.` },
      { id: '15', front: `What are the four FSx types supported by EKS?`, back: `1. FSx for Lustre — HPC, ML, high-throughput parallel access 2. FSx for Windows File Server — SMB/NTFS, Windows workloads 3. FSx for NetApp ONTAP — enterprise NAS features, multi-protocol 4. FSx for OpenZFS — ZFS features, low-latency NFS access` },
      { id: '16', front: `What is the Container Storage Interface (CSI)?`, back: `CSI is a standardized interface between container orchestrators (Kubernetes) and storage providers. Before CSI, storage code was baked into the Kubernetes source. Now, each storage vendor ships their own CSI driver — installed separately — and Kubernetes calls it through the standard API.` },
      { id: '17', front: `What is the provisioner string for the EBS CSI driver?`, back: `ebs.csi.aws.com Used in the StorageClass.provisioner field. Similarly for EFS: efs.csi.aws.com . These strings tell the dynamic provisioner which CSI driver to call when a PVC is created.` },
      { id: '18', front: `How does the EBS CSI driver authenticate to AWS APIs?`, back: `Via IRSA (IAM Roles for Service Accounts) . The EBS CSI driver runs as a pod with a Kubernetes service account annotated with an IAM role ARN. The role has permissions to call EC2 APIs ( ec2:CreateVolume , ec2:AttachVolume , etc.). No long-lived credentials needed.` },
      { id: '19', front: `How do you install the EBS CSI driver in EKS?`, back: `Install it as an EKS managed add-on : via the AWS Console, eksctl , or the AWS CLI ( aws eks create-addon --addon-name aws-ebs-csi-driver ). EKS handles version upgrades. You must also configure an IRSA role for the driver's service account before or after installation.` },
      { id: '20', front: `KC scenario: a MySQL StatefulSet has 3 replicas. Each needs its own 20Gi database volume that survives pod restarts and can grow. What storage?`, back: `Amazon EBS (gp3) with allowVolumeExpansion: true in the StorageClass. Each StatefulSet replica gets its own PVC via volumeClaimTemplates → one EBS volume per pod, independent lifecycle, expandable. EFS would be shared across all replicas (wrong for database isolation).` },
      { id: '21', front: `KC scenario: a content management system has 10 web server pods that all need to read/write shared media files. What storage?`, back: `Amazon EFS — ReadWriteMany access mode lets all 10 pods mount the same EFS file system simultaneously. EBS (RWO) can only be mounted by one node at a time and would not allow shared writes across replicas.` },
      { id: '22', front: `KC — What is the default behavior for Kubernetes Secrets? A. Plaintext in etcd B. AES-256 encrypted C. base64 encoded, stored in etcd on encrypted volume D. Stored in AWS KMS`, back: `✓ C — base64 encoded, stored in etcd on encrypted volume The etcd volume is encrypted (disk-level), but the secret values themselves are only base64 encoded — not encrypted. KMS envelope encryption (option D) is an optional additional layer you can enable, but it is not the default.` },
      { id: '23', front: `Why is "base64 encoded" NOT the same as "encrypted"?`, back: `Base64 is an encoding scheme , not an encryption scheme. It has no key and is trivially reversible: echo "dXNlcjpwYXNz" | base64 -d → user:pass . Anyone who can read the etcd data or the Kubernetes API can immediately decode any Secret value without any key material.` },
      { id: '24', front: `How can you add true encryption for Kubernetes Secrets at rest?`, back: `Enable KMS envelope encryption for etcd. In EKS, configure a EncryptionConfig with an AWS KMS key ARN when creating the cluster. Kubernetes then encrypts secret values with a data key, and AWS KMS protects the envelope key. This adds a true encryption layer on top of the base64 encoding.` },
      { id: '25', front: `What are the two ways to expose a Kubernetes Secret to a pod?`, back: `1. Environment variable — env.valueFrom.secretKeyRef . Simple, but the value is visible in kubectl describe pod and in the process environment. 2. Mounted volume (file) — volumes[].secret.secretName + volumeMounts . The secret value appears as a file in the container; more secure and supports automatic rotation with ASCP.` },
      { id: '26', front: `What is ASCP and what problem does it solve?`, back: `ASCP (AWS Secrets Configuration Provider) is a plugin for the Secrets Store CSI Driver that fetches secrets from AWS Secrets Manager or SSM Parameter Store and delivers them to pods as volume-mounted files or as Kubernetes Secret objects. It solves the problem of storing sensitive data in Kubernetes etcd — secrets live in AWS with proper encryption and rotation.` },
      { id: '27', front: `How does ASCP restrict which pods can access a specific AWS secret?`, back: `Via IRSA (IAM Roles for Service Accounts) . The pod's service account is annotated with an IAM role ARN. The IAM role has a policy that permits secretsmanager:GetSecretValue for only specific secret ARNs. Only pods with that service account can retrieve those secrets — no other pod can.` },
      { id: '28', front: `What is a SecretProviderClass?`, back: `A SecretProviderClass is a custom Kubernetes resource (CRD) that defines which secrets to fetch from AWS. It specifies: the provider: aws , the secret names/paths in Secrets Manager or SSM, and optional sync-to-Kubernetes-Secret configuration. Each pod that needs the secrets references a SecretProviderClass by name in its volume spec.` },
      { id: '29', front: `What CSI driver name does the Secrets Store use?`, back: `secrets-store.csi.k8s.io Referenced in the pod's volume spec under volumes[].csi.driver . This is separate from the EBS CSI driver ( ebs.csi.aws.com ) and EFS CSI driver ( efs.csi.aws.com ) — it is a general-purpose secrets bridge, not a storage driver.` },
      { id: '30', front: `What two delivery modes can ASCP use to expose a secret to a pod?`, back: `1. Volume mount (file) — the secret value appears as a file at a path in the container (e.g., /mnt/secrets/MyDbPassword ). This is the primary ASCP delivery method. 2. Kubernetes Secret sync — ASCP can optionally create a Kubernetes Secret object that mirrors the AWS secret, so existing code that reads from env vars or K8s Secrets continues to work unchanged.` },
      { id: '31', front: `Does ASCP support automatic secret rotation?`, back: `Yes. ASCP periodically re-fetches secrets from Secrets Manager. When AWS Secrets Manager rotates a secret, ASCP will pick up the new value on its next refresh cycle and update the mounted file. The application can re-read the file without restarting. This is a significant advantage over baking secrets into Kubernetes Secret objects manually.` },
      { id: '32', front: `KC — What is the default behavior for a Kubernetes Secret? A. Plaintext B. AES-256 encrypted C. base64 encoded, stored in etcd on encrypted volume D. AWS KMS encrypted`, back: `✓ C — base64 encoded, stored in etcd on encrypted volume. The etcd disk is encrypted but the secret values are only base64 encoded — reversible without a key. KMS encryption (D) is an optional add-on, not the default.` },
      { id: '33', front: `Which Kubernetes workload controller gives each pod a stable, persistent identity?`, back: `StatefulSet. Each pod gets an ordinal name ( name-0 , name-1 ) and its own PersistentVolumeClaim that survives pod deletion. Deployments give pods random names and share (or forgo) PVCs.` },
      { id: '34', front: `You need persistent storage for a Fargate pod. Which AWS storage service must you use?`, back: `Amazon EFS. EBS requires EC2 instance attachment and is not available on Fargate. EFS uses NFS over the network, which works with Fargate pods. Ensure you use the EFS CSI driver and configure an EFS access point per PVC.` },
      { id: '35', front: `What does the ASCP use to ensure only specific pods can access a secret?`, back: `IRSA (IAM Roles for Service Accounts). The pod's Kubernetes service account is annotated with an IAM role. The IAM policy grants secretsmanager:GetSecretValue only for specific secret ARNs. Pods without that service account cannot retrieve the secret.` },
      { id: '36', front: `What is the access mode for Amazon EBS when used with Kubernetes?`, back: `ReadWriteOnce (RWO) — one node can mount the volume read-write at a time. EBS is block storage and must be attached to a single EC2 instance. If you need multiple pods to write simultaneously, use EFS (ReadWriteMany).` },
      { id: '37', front: `What is the provisioner name for the Amazon EBS CSI driver?`, back: `ebs.csi.aws.com — referenced in the StorageClass provisioner field. EFS: efs.csi.aws.com Secrets Store: secrets-store.csi.k8s.io` },
      { id: '38', front: `Lab 5 preview: what does Lab 5 exercise?`, back: `Lab 5 exercises the full PVC lifecycle : (1) create a PVC using pre-created StorageClasses, (2) assign the PVC to a pod, (3) write data through the mounted volume, (4) delete and recreate the pod to confirm data persists, (5) manage storage lifecycle — cleanup and verification.` },
    ],
  },
  {
    id: 'eks-coreks-m09',
    title: 'EKS M09: Managing Security',
    description: '4Cs, IAM, RBAC, OIDC, aws-auth, Service Accounts, IRSA',
    category: 'eks-coreks',
    cardCount: 47,
    color: '#E74C3C',
    icon: '🔐',
    cards: [
      { id: '1', front: `What are the 4Cs of cloud-native security?`, back: `Cloud, Cluster, Container, Code — a defense-in-depth model where each C wraps the one inside it. A vulnerability in an outer layer can expose inner layers. All four must be secured independently.` },
      { id: '2', front: `What does the "Cloud" layer of the 4Cs cover?`, back: `AWS infrastructure security : VPC configuration, IAM policies, account-level guardrails, physical security of data centers. This is partly shared with AWS (physical/hypervisor) and partly your responsibility (VPC design, IAM).` },
      { id: '3', front: `What does the "Cluster" layer of the 4Cs cover?`, back: `Kubernetes cluster security : RBAC (who can call the K8s API), admission controllers (enforce policies at creation time), network policies (pod-to-pod traffic), secrets encryption at rest in etcd, audit logging.` },
      { id: '4', front: `What does the "Container" layer of the 4Cs cover?`, back: `Container image security : scanning images for CVEs (ECR image scanning), using minimal base images, running as non-root, setting readOnlyRootFilesystem: true , dropping Linux capabilities, avoiding privileged containers.` },
      { id: '5', front: `What does the "Code" layer of the 4Cs cover?`, back: `Application code security : SAST (static analysis), dependency scanning (Dependabot, Snyk), input validation to prevent injection attacks, avoiding hardcoded secrets, secure communication (mTLS between services).` },
      { id: '6', front: `Why is the 4Cs model called "defense in depth"?`, back: `Because each layer provides an independent security boundary. An attacker who breaches the Cloud layer still faces Cluster controls. A container escape still faces Code-level protections. No single layer failure should compromise everything. Security comes from multiple overlapping controls .` },
      { id: '7', front: `What extra responsibility does AWS take on with managed node groups vs self-managed?`, back: `With managed node groups, AWS additionally manages node OS patching , rolling node replacements, and joining/removing nodes from the cluster. With self-managed nodes, all of that is your responsibility.` },
      { id: '8', front: `What does AWS manage for you when using Fargate?`, back: `On Fargate, AWS manages the entire node layer : provisioning, OS patching, container runtime, node replacement. You only manage pod security context, app code, RBAC, IAM, and network config . The node is completely abstracted.` },
      { id: '9', front: `What security responsibilities ALWAYS remain yours regardless of data plane option?`, back: `Regardless of data plane choice, you always own: RBAC configuration, IAM policies, network config (VPC/security groups), pod security context, application code, secrets management, and logging/monitoring setup . AWS never owns these.` },
      { id: '10', front: `Why does Fargate reduce your attack surface compared to EC2 nodes?`, back: `Fargate provides 1 pod per micro-VM isolation , OS patching by AWS, no shared kernel between pods, and no SSH access to worry about. You cannot misconfigure node security groups, patch timing, or container runtime versions — those risks don't exist for you.` },
      { id: '11', front: `What data source does GuardDuty use for EKS threat detection?`, back: `GuardDuty analyzes EKS audit logs (control plane API server logs) and, with Runtime Monitoring enabled, runtime activity on nodes (process executions, file access, network connections). It does not require you to ship logs to a SIEM first.` },
      { id: '12', front: `Give two examples of EKS threats that GuardDuty can detect.`, back: `1. Privilege escalation — an attacker binding themselves to system:masters to gain cluster-admin. 2. Credential theft — a service account token being used from an unexpected IP or region outside normal cluster traffic.` },
      { id: '13', front: `Does GuardDuty require you to configure rules or write detections?`, back: `No. GuardDuty is a managed threat detection service. AWS maintains the threat intelligence and detection logic. You enable it and receive findings — you do not write rules. This is the key contrast with a self-managed SIEM or custom audit log analysis.` },
      { id: '14', front: `What are the two separate permission systems in EKS and what does each control?`, back: `IAM controls access to AWS APIs — creating clusters, managing node groups, reading ECR, S3, etc. Kubernetes RBAC controls access to the K8s API — creating pods, listing services, reading secrets within the cluster.` },
      { id: '15', front: `How does authentication work when a developer runs kubectl against EKS?`, back: `1. kubectl calls aws eks get-token to get a bearer token using IAM credentials. 2. Token sent to K8s API server. 3. API server validates token via AWS IAM Authenticator . 4. Validated identity looked up in aws-auth ConfigMap → K8s username/groups assigned. 5. RBAC evaluates what those groups can do.` },
      { id: '16', front: `What is an EKS OIDC issuer and why does it matter for security?`, back: `Each EKS cluster has an OIDC issuer URL . It allows AWS IAM to trust tokens issued by the cluster's K8s API server. This is the cryptographic foundation that enables IRSA — pods can prove their identity to AWS STS without needing static credentials.` },
      { id: '17', front: `Can you have full IAM permissions but still be blocked by RBAC?`, back: `Yes. IAM controls AWS API access; RBAC controls K8s API access. Even if your IAM policy allows eks:* , you still need the appropriate RBAC bindings to run kubectl commands. Both systems must grant access — they operate independently.` },
      { id: '18', front: `What is the difference between a Role and a ClusterRole?`, back: `Role is namespaced — it grants permissions within one namespace only. ClusterRole is cluster-wide — it grants permissions across all namespaces, or to non-namespaced resources like nodes. A ClusterRole can be bound into a namespace using a RoleBinding.` },
      { id: '19', front: `What is the difference between RoleBinding and ClusterRoleBinding?`, back: `RoleBinding is namespace-scoped — applies in one namespace. It can bind a Role OR a ClusterRole (which limits the ClusterRole to that namespace). ClusterRoleBinding is cluster-wide — applies across all namespaces. Can only bind a ClusterRole.` },
      { id: '20', front: `Can you use a RoleBinding to bind a ClusterRole?`, back: `Yes. A RoleBinding can reference a ClusterRole as its roleRef . The effect is that the ClusterRole's permissions apply only within the RoleBinding's namespace — not cluster-wide. This is useful for reusing a ClusterRole definition with namespace-limited scope.` },
      { id: '21', front: `What is the RBAC analogy to IAM? Map each component.`, back: `IAM Policy ≈ RBAC Role/ClusterRole (list of allowed actions + resources) IAM Principal (user/role) ≈ K8s Subject (User/Group/ServiceAccount) IAM Policy Attachment ≈ RoleBinding/ClusterRoleBinding` },
      { id: '22', front: `What is the cluster-admin ClusterRole and who should have it?`, back: `cluster-admin is the default ClusterRole granting full access to all K8s resources . By default, only the IAM entity that created the EKS cluster has it. It should be tightly controlled — do not routinely grant it. Use least-privilege roles instead.` },
      { id: '23', front: `What are the three default RBAC groups in EKS and what do they map to?`, back: `system:masters → cluster-admin ClusterRole (full access) system:nodes → system:node ClusterRole (node operations) system:bootstrappers → bootstrap token role (node join only)` },
      { id: '24', front: `What RBAC verbs exist and what do they map to in HTTP?`, back: `get → GET single resource list → GET collection watch → GET with watch stream create → POST update → PUT patch → PATCH delete → DELETE deletecollection → DELETE collection` },
      { id: '25', front: `What is the aws-auth ConfigMap and where does it live?`, back: `The aws-auth ConfigMap lives in the kube-system namespace. It maps IAM roles/users (AWS identities) to Kubernetes usernames and groups (K8s RBAC subjects). It is the bridge between IAM authentication and RBAC authorization.` },
      { id: '26', front: `What is the two-step process to grant an IAM role K8s permissions?`, back: `Step 1: Add entry to aws-auth ConfigMap: map IAM role ARN → K8s username + groups. Step 2: Create a ClusterRoleBinding (or RoleBinding) that binds the K8s group to a ClusterRole (or Role). Now: IAM role → K8s group → RBAC permissions.` },
      { id: '27', front: `Why must node IAM roles be in aws-auth?`, back: `Worker nodes use an IAM role for AWS API calls, but they also need K8s API access so kubelet can register as a node and report status. The node IAM role must be in aws-auth mapped to system:nodes group so the K8s API server trusts the node's identity.` },
      { id: '28', front: `What happens if you accidentally delete the aws-auth ConfigMap?`, back: `Only the cluster creator's IAM entity retains cluster-admin access (it's hardcoded). All other users lose K8s API access — including your admin roles. Nodes will also lose their K8s identity. You must recreate the ConfigMap immediately using the creator's credentials.` },
      { id: '29', front: `How do you grant a new IAM role cluster-admin in aws-auth?`, back: `Add to the mapRoles section of aws-auth: rolearn: IAM-role-ARN username: cluster-admin groups: [system:masters] The system:masters group is pre-bound to the cluster-admin ClusterRole, giving full access.` },
      { id: '30', front: `What is a Kubernetes service account?`, back: `A Kubernetes identity for pods (not humans). When a pod calls the K8s API, it authenticates using its service account's token. Service accounts are namespace-scoped and their RBAC bindings define what K8s operations the pod can perform.` },
      { id: '31', front: `What is the "default" service account risk?`, back: `Every namespace has a default SA. All pods without an explicit SA use it. If you grant the default SA elevated permissions, all pods in that namespace inherit those permissions — including ones that shouldn't have them. Always create purpose-specific service accounts.` },
      { id: '32', front: `Where is the service account token automatically mounted in a pod?`, back: `Kubernetes automatically projects the token to: /var/run/secrets/kubernetes.io/serviceaccount/token Also mounted: ca.crt (cluster CA) and namespace . Applications use this token to authenticate to the K8s API. You can also disable auto-mounting with automountServiceAccountToken: false .` },
      { id: '33', front: `Can a service account call AWS APIs directly (without IRSA)?`, back: `Not with proper scoping. Without IRSA, a pod would inherit the node's IAM role — meaning all pods on the same node share the same AWS permissions. This is a security anti-pattern. IRSA solves this by letting each service account have its own scoped IAM role.` },
      { id: '34', front: `What problem does IRSA solve?`, back: `Without IRSA, pods use the node's IAM role — all pods on a node share the same AWS permissions (over-permissioned). IRSA lets each pod use a scoped IAM role specific to its service account, following the principle of least privilege.` },
      { id: '35', front: `What is the "three-way trust" in IRSA?`, back: `1. EKS OIDC issuer — issues signed JWT tokens for service accounts 2. IAM — trusts the OIDC issuer (configured via OIDC identity provider) 3. K8s service account — annotated with the IAM role ARN These three form the chain that allows AWS STS to issue temporary credentials to the pod.` },
      { id: '36', front: `How does a pod actually receive AWS credentials via IRSA?`, back: `The Pod Mutating Admission Webhook automatically injects the OIDC token as a projected volume and sets environment variables AWS_WEB_IDENTITY_TOKEN_FILE and AWS_ROLE_ARN . The AWS SDK reads these automatically and calls sts:AssumeRoleWithWebIdentity to get temporary credentials.` },
      { id: '37', front: `What annotation do you add to a K8s service account to enable IRSA?`, back: `eks.amazonaws.com/role-arn: arn:aws:iam:: account-id :role/ role-name This tells EKS which IAM role to assume when pods using this service account call AWS APIs. The IAM role's trust policy must reference this specific namespace and SA name.` },
      { id: '38', front: `What condition must the IAM trust policy have for IRSA to be secure?`, back: `The trust policy StringEquals condition must specify the exact OIDC issuer URL + SA subject : "system:serviceaccount: namespace : sa-name " . Without this, any SA in the cluster could assume the role. The specificity of the subject claim is what makes IRSA secure.` },
      { id: '39', front: `What is the first step to enable IRSA on an existing EKS cluster?`, back: `Associate the cluster's OIDC issuer URL with IAM: eksctl utils associate-iam-oidc-provider --cluster name --approve Or via Console: EKS cluster → Configuration → Authentication → Add OIDC. This creates the IAM OIDC identity provider that allows IAM to trust the cluster's service account tokens.` },
      { id: '40', front: `KC 1 — How does the EKS cluster creator get cluster-admin access? (Select 2) A. Auto system:masters B. Default RBAC policy C. Modify aws-auth D. Auto cluster-admin CRB`, back: `✓ A C A: The IAM entity that creates the cluster is automatically added to system:masters (no aws-auth entry needed for them). C: Additional users get access by modifying the aws-auth ConfigMap to map their IAM role/user to a K8s group, then adding a ClusterRoleBinding.` },
      { id: '41', front: `KC 2 — Which is the MOST restrictive policy allowing a pod to read from S3? A. Admin IAM policy on node B. S3ReadOnly on node C. Admin IAM via IRSA D. Custom S3 role via IRSA`, back: `✓ D — Custom IAM role with only required S3 permissions via IRSA IRSA with a purpose-built, minimally scoped IAM policy is least-privilege. Options A/B use the node role (all pods on node share), Option C uses IRSA but grants excessive admin permissions.` },
      { id: '42', front: `KC 3 — What do you need for a user to deploy pods to an EKS cluster? (Select 3) A. kubectl + kubeconfig B. IAM perms for K8s API C. RBAC Role for pod creation D. Root account access`, back: `✓ A, B, C A: kubectl tool + kubeconfig pointing to the cluster. B: IAM permissions to authenticate (get token for the K8s API). C: RBAC Role/ClusterRole granting create on pods (or deployments ), bound via RoleBinding/ClusterRoleBinding. D is wrong — root access is not needed or recommended.` },
      { id: '43', front: `KC 4 — Service account permissions. Which statements are correct? (Select A,B,D,E)`, back: `Correct: A — SA is K8s identity for pods. B — SA enables pod→K8s API auth. D — IRSA lets SA assume IAM role for AWS API calls. E — SA token auto-projected into pod. Incorrect: C — Default SA does NOT have cluster-admin. Never grant broad permissions to default SA.` },
      { id: '44', front: `Exam trap: A developer has full AdministratorAccess IAM policy. They run kubectl get pods and get "Forbidden". Why?`, back: `IAM controls AWS API access, not K8s API access. The developer's IAM role is not in the aws-auth ConfigMap, so they have no K8s identity. They need to be added to aws-auth (mapped to a K8s group) AND have an appropriate RBAC binding. AdministratorAccess does not grant K8s access.` },
      { id: '45', front: `Exam trap: Can a ClusterRole be used to grant namespace-scoped permissions?`, back: `Yes. If you bind a ClusterRole using a RoleBinding (not ClusterRoleBinding) in a specific namespace, the ClusterRole's permissions apply only to that namespace. This is a valid pattern for reusing permission definitions without creating duplicate Role objects in every namespace.` },
      { id: '46', front: `What is the security risk of using the node IAM role for pod AWS access?`, back: `Blast radius. All pods on a node share the node's IAM role. If any pod is compromised, the attacker gains access to all AWS resources the node role can reach — not just those needed by that pod. IRSA scopes credentials per SA, containing the blast radius to a single service.` },
      { id: '47', front: `Where in the 4Cs model does RBAC belong?`, back: `RBAC belongs in the Cluster layer — the second C. It is a Kubernetes-layer control, not AWS infrastructure (Cloud), not container image security (Container), and not application code (Code). Cluster security includes RBAC, admission controllers, network policies, and audit logging.` },
    ],
  },
];
